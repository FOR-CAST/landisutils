# Specify `DOMPools` for Forest Carbon Succession (ForCS) extension

Specify `DOMPools` for Forest Carbon Succession (ForCS) extension

## Usage

``` r
insertDOMPools(df)
```

## Arguments

- df:

  data.frame corresponding to `DOMPools` table

## Value

data.frame
