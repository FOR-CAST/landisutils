# Prepare Original Fire Extension `FireRegionParameters` Table

Prepare Original Fire Extension `FireRegionParameters` Table

## Usage

``` r
prepFireRegionParametersTable(sf)
```

## Arguments

- sf:

  `sf` polygon object

## Value

data.frame
