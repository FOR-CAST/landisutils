# Specify `AvailableLightBiomass` for Forest Carbon Succession (ForCS) extension

Specify `AvailableLightBiomass` for Forest Carbon Succession (ForCS)
extension

## Usage

``` r
insertAvailableLightBiomass(df)
```

## Arguments

- df:

  data.frame corresponding to `AvailableLightBiomass` table

## Value

data.frame
