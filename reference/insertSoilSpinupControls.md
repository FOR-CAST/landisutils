# Specify `SoilSpinup` for Forest Carbon Succession (ForCS) extension

Specify `SoilSpinup` for Forest Carbon Succession (ForCS) extension

## Usage

``` r
insertSoilSpinupControls(df)
```

## Arguments

- df:

  data.frame corresponding to `SoilSpinup` table

## Value

data.frame
