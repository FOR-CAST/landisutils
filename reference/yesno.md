# Convert logical to "yes" or "no"

LANDIS-II input files use character strings "yes" and "no" instead of
logical TRUE or FALSE.

## Usage

``` r
yesno(x)
```

## Arguments

- x:

  one of `TRUE`, `FALSE`, `Y`, `N`, `yes`, `no`.

## Value

Character "yes" or "no".
