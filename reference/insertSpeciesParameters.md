# Specify `SpeciesParameters` for Forest Carbon Succession (ForCS) extension

Specify `SpeciesParameters` for Forest Carbon Succession (ForCS)
extension

## Usage

``` r
insertSpeciesParameters(df)
```

## Arguments

- df:

  data.frame corresponding to `SpeciesParameters` table

## Value

data.frame
