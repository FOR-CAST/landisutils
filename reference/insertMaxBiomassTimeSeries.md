# Specify `MaxBiomassTimeSeries` for Forest Carbon Succession (ForCS) extension

Specify `MaxBiomassTimeSeries` for Forest Carbon Succession (ForCS)
extension

## Usage

``` r
insertMaxBiomassTimeSeries(df)
```

## Arguments

- df:

  data.frame corresponding to `MaxBiomassTimeSeries` table

## Value

data.frame
