# Specify `LightEstablishmentTable` for Forest Carbon Succession (ForCS) extension

Specify `LightEstablishmentTable` for Forest Carbon Succession (ForCS)
extension

## Usage

``` r
insertLightEstablishmentTable(df)
```

## Arguments

- df:

  data.frame corresponding to `LightEstablishmentTable` table

## Value

data.frame
