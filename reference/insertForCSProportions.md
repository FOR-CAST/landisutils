# Specify `ForCSProportions` for Forest Carbon Succession (ForCS) extension

Specify `ForCSProportions` for Forest Carbon Succession (ForCS)
extension

## Usage

``` r
insertForCSProportions(df)
```

## Arguments

- df:

  data.frame corresponding to `ForCSProportions` table

## Value

data.frame
