# Specify `OutputTables` for Forest Carbon Succession (ForCS) extension

Specify `OutputTables` for Forest Carbon Succession (ForCS) extension

## Usage

``` r
insertOutputTables(df = NULL)
```

## Arguments

- df:

  data.frame corresponding to `ForCSOutput` table

## Value

data.frame
