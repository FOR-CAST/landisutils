# Specify Biomass Succession Extension `SufficientLight` Table

Specify Biomass Succession Extension `SufficientLight` Table

## Usage

``` r
insertSufficientLight(df)
```

## Arguments

- df:

  data.frame corresponding to the `SufficientLight` table

## Value

Character string(s) to write to the suitable LANDIS-II input file.
