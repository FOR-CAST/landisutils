# Specify `RootDynamics` for Forest Carbon Succession (ForCS) extension

Specify `RootDynamics` for Forest Carbon Succession (ForCS) extension

## Usage

``` r
insertRootDynamics(df)
```

## Arguments

- df:

  data.frame corresponding to `RootDynamics` table

## Value

data.frame
