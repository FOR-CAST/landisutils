# Specify Scenario `RandomNumberSeed`

Optional.

## Usage

``` r
insertRandomNumberSeed(seed)
```

## Arguments

- seed:

  Integer (\>0). Seed to initialize LANDIS-II random number generator.

## Value

Character string(s) specifying suitable LANDIS-II input file(s), created
as a side effect of the function.
