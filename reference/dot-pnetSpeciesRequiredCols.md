# Required `PnETSpeciesParameters` columns

Required columns of the `PnETSpeciesParameters` input file (§8 of the
user guide). The first column gives the species name; remaining columns
are physiological parameters that vary by species.

## Usage

``` r
.pnetSpeciesRequiredCols
```

## Format

An object of class `character` of length 16.
