# an oversized design errors rather than building an enormous landscape

    Code
      growth_calibration_design(params(), grid = grid, max_cells = 100L)
    Condition
      Error:
      ! growth_calibration_design(): 6401 pseudo-species x 1 ecoregions = 6401 cells exceeds max_cells = 100. Narrow the sweep in the design CSV, or raise max_cells deliberately.

