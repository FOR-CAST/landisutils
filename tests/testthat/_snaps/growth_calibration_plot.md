# plot_growth_factorial_sensitivity drops parameters the design did not sweep

    Code
      plot_growth_factorial_sensitivity(scores, current)
    Condition
      Error in `plot_growth_factorial_sensitivity()`:
      ! no swept parameter in `scores`: every candidate column holds a single value.

