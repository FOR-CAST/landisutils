% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_dynamic_fire.R
\name{sim_landis}
\alias{sim_landis}
\title{Run one DEoptim trial for a candidate parameter vector (blocking)}
\usage{
sim_landis(
  par_vec,
  par_names = NULL,
  paths,
  sim_years,
  base_seed,
  pool = NULL,
  pool_idx = NULL,
  method = NULL,
  pixel_area_ha = 1,
  keep_scratch = FALSE
)
}
\arguments{
\item{par_vec}{Numeric. Named candidate parameter vector.}

\item{par_names}{Character or NULL. Names in canonical order
(\code{\link[=calibration_par_names]{calibration_par_names()}}). Used to re-attach names if DEoptim strips
them when calling the objective function with positional args.}

\item{paths}{Named list of strings. Required entries:
\describe{
\item{scenario_template}{Directory built by
\code{\link[=build_calibration_scenario_template]{build_calibration_scenario_template()}}.}
\item{scratch_root}{Where per-trial dirs are created. Must equal the
pool's \code{scratch_root} when \code{pool} is supplied. NULL = \code{tempdir()}.}
}}

\item{sim_years}{Integer. Calibration sim duration in years (informational;
the actual Duration comes from the template's scenario.txt).}

\item{base_seed}{Integer. Random seed for this trial.}

\item{pool}{A \code{landis_pool} from \code{\link[=landis_pool_start]{landis_pool_start()}}, or NULL for one-off.}

\item{pool_idx}{Integer. 1-based container index in \code{pool}. Required when
\code{pool} is non-NULL.}

\item{method}{Character. \code{"docker"} or \code{"local"}. Used only when \code{pool} is
NULL. Default from \code{getOption("landisutils.run.method")}.}

\item{pixel_area_ha}{Numeric. Hectares per cell. Default 1.}

\item{keep_scratch}{Logical. Leave the per-trial scratch dir in place for
debugging. Default FALSE.}
}
\value{
The output of \code{\link[=parse_dynamic_fire_logs]{parse_dynamic_fire_logs()}} for the trial's \verb{rep01/}.
}
\description{
One calibration trial: copy \code{paths$scenario_template} into a scratch dir
under \code{paths$scratch_root}, patch \code{dynamic-fire.txt} with \code{par_vec}, run
LANDIS-II via the warm Docker pool (or a one-off Docker/local invocation if
no pool is supplied), parse the resulting Dynamic Fire logs.
}
\details{
\code{paths} carries only file PATHS so the function is FORK-safe (no terra/sf
objects in the worker's environment).

Isolation between trials in the same pool container: each trial uses a
unique scratch directory; \code{landis_pool_exec()} sets per-call env vars to
redirect dotnet caches; the trial directory is deleted after parsing unless
\code{keep_scratch = TRUE}.
}
\seealso{
Other Dynamic Fire calibration helpers:
\code{\link[=apply_calibrated_hi_prop]{apply_calibrated_hi_prop()}},
\code{\link[=apply_calibrated_ignprob]{apply_calibrated_ignprob()}},
\code{\link[=bc_fuel_code_to_base]{bc_fuel_code_to_base()}},
\code{\link[=build_calibration_scenario_template]{build_calibration_scenario_template()}},
\code{\link[=build_calibration_spinup_scenario]{build_calibration_spinup_scenario()}},
\code{\link[=calibrate_dynamic_fire]{calibrate_dynamic_fire()}},
\code{\link[=calibration_par_names]{calibration_par_names()}},
\code{\link[=loss_from_stats]{loss_from_stats()}},
\code{\link[=parse_dynamic_fire_logs]{parse_dynamic_fire_logs()}},
\code{\link[=patch_fire_config]{patch_fire_config()}},
\code{\link[=run_calibration_spinup]{run_calibration_spinup()}},
\code{\link[=save_observed_fire_targets]{save_observed_fire_targets()}},
\code{\link[=sim_mock]{sim_mock()}},
\code{\link[=sim_r_reimpl]{sim_r_reimpl()}}
}
\concept{Dynamic Fire calibration helpers}
