% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landis_datatype.R
\name{landis_datatype}
\alias{landis_datatype}
\title{Pick a raster pixel type LANDIS-II can read}
\usage{
landis_datatype(max_value)
}
\arguments{
\item{max_value}{Largest value the map has to represent. Must be finite and
non-negative.}
}
\value{
A length-1 character \code{datatype} suitable for \code{\link[terra:writeRaster]{terra::writeRaster()}}:
\code{"INT1U"}, \code{"INT2S"} or \code{"INT4S"}.
}
\description{
Returns the smallest terra \code{datatype} that LANDIS-II will open and that can
hold \code{max_value}. Use it for any integer-coded map written for LANDIS-II
(initial communities, ecoregions, fire regions), rather than choosing a
datatype by hand.
}
\details{
LANDIS-II opens maps through
\code{Landis.RasterIO.Gdal.GdalInputRaster.NewInputBand}, which accepts only GDAL
\code{Byte}, \code{Int16}, \code{Int32}, \code{Float32} and \code{Float64}, and aborts the run with
\verb{Raster band is not byte, short, int, float, double} on anything else. The
unsigned integer types are therefore unusable even though map codes are
always positive -- as is \code{INT1S}, which GDAL 3.7+ writes as \code{Int8}. Because
the check happens when the extension initialises rather than when the file is
written, an unreadable map is only discovered by running the model.
}
\examples{
landis_datatype(200) ## "INT1U"
landis_datatype(5000) ## "INT2S" -- NOT "INT2U", which LANDIS-II rejects
landis_datatype(1e6) ## "INT4S"

}
