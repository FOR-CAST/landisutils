% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.need}
\alias{.need}
\title{Require an optional (\code{Suggests}) package, with an actionable error}
\usage{
.need(pkg, what)
}
\arguments{
\item{pkg}{Character. The package needed.}

\item{what}{Character. What the caller was trying to do, phrased to read after
a subject: \code{"Reading a parquet dataset"}.}
}
\value{
Invisibly \code{TRUE}; called for its side effect of stopping.
}
\description{
Several capabilities here rest on packages that most users of this one never
touch: reading Arrow datasets, drawing plots, recomputing fire weather. Those
live in \code{Suggests} rather than \code{Imports} so installing this package does not
drag them in -- \code{arrow} alone is 99 MB, and \code{cffdrs} pulls 16 packages nothing
else needs. Call this at the top of any exported function that reaches one, so
a missing package produces a message naming what to install rather than a bare
"there is no package called ..." from somewhere deep in the call stack.
}
\keyword{internal}
