% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration_plots.R
\name{plot_calibration_severity}
\alias{plot_calibration_severity}
\title{Plot observed against simulated burn-severity classes}
\usage{
plot_calibration_severity(
  stats,
  weight_by = c("events", "area"),
  classes = c(3, 5)
)
}
\arguments{
\item{stats}{A \code{run_calibration_validation()} summary carrying
\verb{$observed$primary$severity_dist}.}

\item{weight_by}{\code{"events"} (one vote per fire) or \code{"area"} (weight each
fire by the area it burned). See Weighting.}

\item{classes}{\code{3} to collapse to low/medium/high before plotting -- the
three categories a thresholded observed reference usually distinguishes --
or \code{5} for the raw Dynamic Fire classes.}
}
\value{
A ggplot object.
}
\description{
Plot observed against simulated burn-severity classes
}
\section{Weighting}{

\code{weight_by} is the important argument. The objective's severity term counts
fire EVENTS: it bins each event's mean severity and gives every fire one
vote, whatever its size (\code{weight_by = "events"}, the default, so the figure
matches what was scored). An observed severity reference is almost always a
share of burned AREA -- so many pixels or hectares in each class -- and where
severity rises with fire size the two summaries of the same simulation can
differ sharply, and can even disagree in sign about which way the model is
off. \code{weight_by = "area"} weights each event by the area it burned, which is
the like-for-like comparison against an area-weighted reference.

Both options still summarise each fire by its mean severity, so neither
recovers the within-fire variation that a per-pixel reference retains.
}

\seealso{
Other calibration plots:
\code{\link[=calibration_events]{calibration_events()}},
\code{\link[=calibration_plot_palette]{calibration_plot_palette()}},
\code{\link[=plot_calibration_area_by_fuel]{plot_calibration_area_by_fuel()}},
\code{\link[=plot_calibration_convergence]{plot_calibration_convergence()}},
\code{\link[=plot_calibration_fire_counts]{plot_calibration_fire_counts()}},
\code{\link[=plot_calibration_fire_sizes]{plot_calibration_fire_sizes()}},
\code{\link[=plot_calibration_loss]{plot_calibration_loss()}},
\code{\link[=plot_calibration_severity_by_size]{plot_calibration_severity_by_size()}}
}
\concept{calibration plots}
