% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/climate_daily_biosim.R
\name{create_locations_df}
\alias{create_locations_df}
\title{Build batched point-location table from a study area}
\usage{
create_locations_df(elev, studyArea, id, batch_size = 1000)
}
\arguments{
\item{elev}{\code{SpatRaster} of elevations in lon-lat CRS (typically from
\code{\link[=get_elevation_rast]{get_elevation_rast()}}).}

\item{studyArea}{\code{sf} or \code{SpatVector} polygons object delineating the area
of interest.}

\item{id}{character. Name of the field in \code{studyArea} identifying ecoregions
(e.g. \code{"PolyID"}).}

\item{batch_size}{integer. Maximum cells per batch. Default \code{1000}.}
}
\value{
A list of data frames with columns \code{ID}, \code{Longitude}, \code{Latitude},
\code{Elevation}, \code{EcoID}, \code{BatchID} (one element per batch).
}
\description{
Extracts the lon-lat / elevation of every cell in \code{elev} that overlaps
\code{studyArea}, tags each cell with its containing polygon's \code{id} value
(column \code{EcoID}) and a numeric \code{BatchID}, and splits into a list of
\code{batch_size}-row data frames suitable for chunked dispatch through
\code{\link[=get_clim_daily]{get_clim_daily()}}.
}
\seealso{
\code{\link[=get_elevation_rast]{get_elevation_rast()}}, \code{\link[=get_clim_daily]{get_clim_daily()}}
}
