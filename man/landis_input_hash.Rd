% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landis_input_hash.R
\name{landis_input_hash}
\alias{landis_input_hash}
\title{Input hash of one LANDIS-II replicate}
\usage{
landis_input_hash(
  dep_files,
  base_seed,
  rep_index,
  scenario_file,
  collation = NULL
)
}
\arguments{
\item{dep_files}{Character. The dependency files staged into the replicate.}

\item{base_seed}{Integer. The base random seed.}

\item{rep_index}{Integer. The replicate index.}

\item{scenario_file}{Character(1). The scenario file name.}

\item{collation}{\code{NULL} (the default) for byte order. Otherwise an \code{LC_COLLATE} locale to sort in,
which reproduces the hash landisutils 0.0.149 and earlier wrote in that locale; \code{""} means the
locale the process's environment selects, as when R started.}
}
\value{
Character(1), a SHA-1 digest; \code{NA_character_} if \code{collation} cannot be set.
}
\description{
A digest of what a replicate was run from: the MD5 of every dependency file (keyed by path), the
base seed, the replicate index and the scenario file name. \code{\link[=tar_landis]{tar_landis()}} writes it to
\verb{<rep>/log/input_hash.json} after a run and compares it before the next, via
\code{\link[=landis_rep_is_current]{landis_rep_is_current()}}.
}
\details{
Paths are ordered by bytes (\code{sort(method = "radix")}), so the hash does not depend on the
collation locale of the R process computing it. R collates with ICU in UTF-8 locales and by bytes
under C/POSIX; a locale-sorted hash differs between a worker started with and without \code{LANG}.

R also collates by bytes, whatever \code{\link[=Sys.setlocale]{Sys.setlocale()}} says, while the environment variable
\code{LC_ALL} (or, when that is unset, \code{LC_COLLATE}) is \code{C}. For a named \code{collation} the sort therefore
runs with \code{LC_ALL} unset and \code{LC_COLLATE} set to \code{collation}, as in a process started in that
locale, and both are restored afterwards.
}
