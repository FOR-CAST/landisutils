% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/community_dedup.R
\name{dedup_community_snapshot}
\alias{dedup_community_snapshot}
\title{Collapse duplicate communities in an Output Biomass Community snapshot}
\usage{
dedup_community_snapshot(csv, tif, out_csv = csv, out_tif = tif, quiet = FALSE)
}
\arguments{
\item{csv}{Path to the snapshot CSV (\verb{community-input-file-<t>.csv}), with a
\code{MapCode} column plus the cohort columns (typically \code{SpeciesName},
\code{CohortAge}, \code{CohortBiomass}).}

\item{tif}{Path to the matching map-code raster (\verb{output-community-<t>.tif}).}

\item{out_csv, out_tif}{Output paths. Default to overwriting \code{csv} / \code{tif}.}

\item{quiet}{Suppress the summary message.

A snapshot can also contain \strong{empty communities}: active cells whose map
code has no CSV rows at all (no cohorts -- recently disturbed, or active but
unforested). Biomass Succession emits these itself. They are active landscape,
so they are collapsed to one shared code and left active rather than zeroed;
the active-cell count is asserted unchanged before anything is written.}
}
\value{
Invisibly, a list with \code{csv}, \code{tif}, \code{map_codes_before},
\code{map_codes_after}, \code{rows_before}, \code{rows_after}, \code{empty_code} (the shared
empty-community code, \code{NA} if there were none) and \code{empty_cells}.
}
\description{
Biomass Succession writes its Output Biomass Community state with \strong{one map
code per pixel}: succession is evaluated per cell, so the writer emits a
distinct \code{MapCode} for every active cell and never re-collapses cells whose
cohort lists ended up identical. On a large landscape almost all of those map
codes are duplicates of one another, and the redundancy is carried entirely by
the CSV.
}
\details{
That matters because LANDIS-II reads initial communities back through
\code{Landis.Library.InitialCommunities.Universal.DatasetParser}, which builds a
\code{System.Dynamic.ExpandoObject} per row. The parser's memory cost is a large
multiple of the file size, so a snapshot that is mostly duplicate rows can
exhaust a container's \code{--memory} and abort the run with
\code{System.OutOfMemoryException} inside \code{ReadCSVInputFile}, before the simulation
starts. Measured on a 2.98M-active-cell landscape: 2,684,154 map codes
carrying only 4,153 distinct communities -- a 1,472 MB CSV that deduplicates
to roughly 2 MB.

This rewrites the pair so that one map code represents each \strong{distinct}
community and the raster points every pixel at its community's new code. The
simulated state is unchanged: every pixel still maps to exactly the cohort
list it had before.

Two communities are the same when their cohort sets are identical, compared
on every non-\code{MapCode} column after ordering rows canonically within a map
code. Biomass is compared exactly -- no rounding -- so this never merges
cells that differ, only cells that are already identical.
}
\seealso{
\code{\link[=run_calibration_spinup]{run_calibration_spinup()}}

Other Dynamic Fire calibration helpers:
\code{\link[=apply_calibrated_hi_prop]{apply_calibrated_hi_prop()}},
\code{\link[=apply_calibrated_ignprob]{apply_calibrated_ignprob()}},
\code{\link[=bc_fuel_code_to_base]{bc_fuel_code_to_base()}},
\code{\link[=build_calibration_scenario_template]{build_calibration_scenario_template()}},
\code{\link[=build_calibration_spinup_scenario]{build_calibration_spinup_scenario()}},
\code{\link[=calibrate_dynamic_fire]{calibrate_dynamic_fire()}},
\code{\link[=calibration_par_names]{calibration_par_names()}},
\code{\link[=default_severity_prior_sturtevant2009]{default_severity_prior_sturtevant2009()}},
\code{\link[=loss_from_stats]{loss_from_stats()}},
\code{\link[=parse_dynamic_fire_logs]{parse_dynamic_fire_logs()}},
\code{\link[=patch_fire_config]{patch_fire_config()}},
\code{\link[=run_calibration_spinup]{run_calibration_spinup()}},
\code{\link[=run_calibration_validation]{run_calibration_validation()}},
\code{\link[=save_observed_fire_targets]{save_observed_fire_targets()}},
\code{\link[=sim_landis]{sim_landis()}},
\code{\link[=sim_mock]{sim_mock()}},
\code{\link[=sim_r_reimpl]{sim_r_reimpl()}}
}
\concept{Dynamic Fire calibration helpers}
