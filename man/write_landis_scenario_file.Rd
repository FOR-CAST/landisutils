% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{write_landis_scenario_file}
\alias{write_landis_scenario_file}
\title{Write a LANDIS-II \code{scenario.txt} file (lower-level helper)}
\usage{
write_landis_scenario_file(
  path,
  duration,
  cell_length,
  species_file,
  ecoregions_files,
  succession_ext_files,
  disturbance_ext_files = NULL,
  other_ext_files = NULL,
  output_manifest = character(0)
)
}
\arguments{
\item{path}{Character. Scenario directory path (absolute or relative).}

\item{duration}{Integer. Simulation duration in years.}

\item{cell_length}{Integer. Raster cell size in metres.}

\item{species_file}{Character. Path to the core species input file.}

\item{ecoregions_files}{Character vector of length 2: paths to the
ecoregions text file and the ecoregions raster map.}

\item{succession_ext_files}{Named character vector. Names are extension
names (e.g. \code{"ForC Succession"}); values are absolute paths to the
per-extension init files. Exactly one entry is required.}

\item{disturbance_ext_files, other_ext_files}{Named character vector or
\code{NULL}. Same structure as \code{succession_ext_files}; zero or more entries.}

\item{output_manifest}{Character vector of fixed-name output files (paths
relative to each replicate directory) that the scenario will produce at
run time. Written to \code{output_manifest.txt} so \code{\link[=tar_landis]{tar_landis()}} can track
these files alongside the recursive \code{output_dir} scan.}
}
\value{
Character scalar: absolute path to the written \code{scenario.txt}.
}
\description{
Generates a \code{scenario.txt} in \code{path} using the \verb{insert*()} helpers.
Lower-level than \code{\link[=scenario]{scenario()}}: takes already-written extension config-file
paths (named character vectors keyed by extension name) rather than R6
\link{LandisExtension} objects. Useful when project pipelines write extension
configs in separate \code{targets}-managed steps and just want to assemble the
top-level scenario file from those paths.
}
\details{
The \code{RandomNumberSeed} line is written in commented-out form so that
\code{\link[=landis_replicate]{landis_replicate()}} can overwrite it with a per-replicate deterministic
seed (controlled by its \code{base_seed} argument).
}
\seealso{
\code{\link[=scenario]{scenario()}}

Other LANDIS-II execution helpers:
\code{\link[=host_cpu_info]{host_cpu_info()}},
\code{\link[=landis_find]{landis_find()}},
\code{\link[=landis_find_docker]{landis_find_docker()}},
\code{\link[=landis_pool_exec]{landis_pool_exec()}},
\code{\link[=landis_pool_start]{landis_pool_start()}},
\code{\link[=landis_pool_stop]{landis_pool_stop()}},
\code{\link[=landis_replicate]{landis_replicate()}},
\code{\link[=landis_run_docker]{landis_run_docker()}},
\code{\link[=landis_run_local]{landis_run_local()}},
\code{\link[=read_landis_resource_logs]{read_landis_resource_logs()}},
\code{\link[=tar_landis]{tar_landis()}}
}
\concept{LANDIS-II execution helpers}
