% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tar_landis.R
\name{tar_landis}
\alias{tar_landis}
\title{Create a \code{targets} target that runs one LANDIS-II replicate}
\usage{
tar_landis(
  name,
  scenario_dir,
  rep_index,
  deps = NULL,
  scenario_file = "scenario.txt",
  output_dir = "output",
  method = NULL,
  image = NULL,
  console = NULL,
  base_seed = NULL,
  pull = FALSE,
  force = FALSE,
  cpu_limit = 4,
  mem_limit = "8g",
  mem_margin = 1.5,
  post_completion_timeout_sec = 300,
  work_root = NULL,
  pattern = NULL,
  packages = targets::tar_option_get("packages"),
  library = targets::tar_option_get("library"),
  error = targets::tar_option_get("error"),
  memory = targets::tar_option_get("memory"),
  resources = targets::tar_option_get("resources"),
  storage = targets::tar_option_get("storage"),
  retrieval = targets::tar_option_get("retrieval"),
  cue = targets::tar_option_get("cue"),
  description = targets::tar_option_get("description")
)
}
\arguments{
\item{name}{Symbol (unquoted). Target name.}

\item{scenario_dir}{Symbol or expression (unquoted). Upstream target that
provides the scenario directory path(s) at run time.}

\item{rep_index}{Symbol (unquoted). Upstream target that provides the
1-based integer index for the current branch. Must be defined with
\code{iteration = "vector"} so that \code{cross()} iterates over individual
elements. Typically created as
\code{tar_target(name = ..._rep_index, command = seq_len(n_reps), iteration = "vector")}.}

\item{deps}{List (unquoted, optional). A \code{list()} of upstream target
symbols that must complete before the simulation runs, e.g.
\code{list(landis_scenario_file, landis_ext_forcs_file)}. Values are not used
directly -- they are embedded in the command so \code{{targets}} detects them
as upstream dependencies.}

\item{scenario_file}{Character. Scenario filename inside \code{scenario_dir}.}

\item{output_dir}{Character vector. Output subdirectory (or subdirectories)
inside \code{scenario_dir}; all files found there (recursively) are returned as
tracked outputs. Defaults to \code{"output"}. Pass \code{c("output", "fire")} when
using the Dynamic Fire extension, which writes its maps and logs to a
\verb{fire/} subdirectory.}

\item{method}{Character or \code{NULL}. \code{"docker"} to run in Docker,
\code{"local"} to run via a local \code{dotnet} installation. \code{NULL} (default)
reads \code{getOption("landisutils.run.method")}, which itself defaults to
\code{"local"} on Windows and \code{"docker"} on Linux/macOS.}

\item{image}{Character or \code{NULL}. Docker image to use
(\code{method = "docker"} only). Resolved from
\code{getOption("landisutils.docker.image")} when \code{NULL}.}

\item{console}{Character or \code{NULL}. Path to \code{Landis.Console.dll}.
For \code{method = "docker"} this is the path \emph{inside} the container;
for \code{method = "local"} it is the local filesystem path (defaults to
\code{\link[=landis_find]{landis_find()}}).}

\item{base_seed}{Integer or \code{NULL}. Passed to \code{\link[=landis_replicate]{landis_replicate()}}: when
non-\code{NULL}, the \code{RandomNumberSeed} in each replicate's \code{scenario.txt} is
set to \code{base_seed + (rep_index - 1)}, giving each run a distinct but
deterministic seed. Adding more replicates later never changes existing
seeds because seeds are derived from the rep index, not the order of
creation.}

\item{pull}{Logical. Passed to \code{\link[=landis_run_docker]{landis_run_docker()}} when
\code{method = "docker"}: when \code{TRUE}, the image is \verb{docker pull}ed before
running so the digest captured in \code{log/docker_image.log} reflects the
current registry. Defaults to \code{FALSE}. No effect for \code{method = "local"}.}

\item{force}{Logical (default \code{FALSE}). When \code{FALSE}, \code{tar_landis()} skips
the actual \verb{landis_run_*()} call if the rep dir already contains a
completed \code{Landis-log.txt} \emph{and} a \code{log/input_hash.json} sidecar whose
recorded hash matches the current inputs (per-input-file MD5 + \code{base_seed}
\itemize{
\item \code{rep_index} + \code{scenario_file}). When \code{TRUE}, the skip check is bypassed
and LANDIS-II is invoked unconditionally.
}}

\item{cpu_limit, mem_limit, mem_margin}{Passed to \code{\link[=landis_run_docker]{landis_run_docker()}} when
\code{method = "docker"}. See that function's documentation for semantics;
defaults are \code{4}, \code{"8g"}, and \code{1.5} respectively. No effect for
\code{method = "local"}.}

\item{post_completion_timeout_sec}{Numeric. Passed to \code{\link[=landis_run_docker]{landis_run_docker()}}
when \code{method = "docker"}: grace period (seconds) after the LANDIS-II
console logs \code{"Model run is complete."} before the container is SIGTERMed
if \code{dotnet} has not exited on its own. Guards against the known v8
post-completion hang (long ForCS + Dynamic Fire sims spin at 100\% CPU in
the .NET shutdown path after outputs are already on disk). Default \code{300}
(5 min); pass \code{Inf} to disable the watchdog. No effect for
\code{method = "local"}.}

\item{work_root}{Character or \code{NULL}. Optional fast, local, Docker-bind-
mountable scratch root used to RUN each replicate, separate from the final
(tracked) \code{scenario_dir}. Needed when \code{scenario_dir} lives on storage the
Docker daemon cannot bind-mount (e.g. a root-squashed NFS share): the rep
is staged + run under \verb{work_root/<scenario>/<studyArea>/repNN}, then its
completed outputs are moved to \code{scenario_dir/repNN} via
\code{\link[=landis_archive_rep]{landis_archive_rep()}} (fault-tolerant \code{rsync} + retry) so the value the
target returns -- and everything \code{{targets}} tracks -- is the FINAL
location, with scratch holding only transient run files. The skip-check
and output collection both read the final \code{scenario_dir/repNN}. When \code{NULL}
(default), the per-run env var \code{LANDIS_SCRATCH} is consulted at run time
(so \code{{crew}} workers can set it via \code{.Rprofile}); when that is also empty
the rep runs in place under \code{scenario_dir} (original behaviour). No effect
for \code{method = "local"}.}

\item{pattern}{Expression (unquoted). Dynamic-branching pattern covering
both the scenario and replicate dimensions, e.g.
\code{cross(landis_run_name, landis_run_output_rep_index)}.
Passed directly to \code{\link[targets:tar_target_raw]{targets::tar_target_raw()}}.}

\item{packages, library, error, memory, resources, storage, retrieval, cue, description}{Standard \code{{targets}} options; all default to \code{\link[targets:tar_option_get]{targets::tar_option_get()}}.}
}
\value{
A single \code{tar_target} object (from \code{\link[targets:tar_target_raw]{targets::tar_target_raw()}}).
}
\description{
A \code{{targets}} factory that creates \strong{one} \code{format = "file"} target.
Each branch runs exactly one LANDIS-II simulation and returns only that
replicate's output files.
}
\details{
\strong{Per-replicate parallel branching:} the caller creates a rep-index target
with \code{iteration = "vector"} and combines it with the scenario dimension via
\code{cross()}. Keeping the rep-index target explicit in the project's \code{list()}
makes it visible to static analysis tools (tarborist) and makes the
\code{iteration = "vector"} annotation clear in the project code:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list(
  ## iteration = "vector" is critical: cross() iterates over each ELEMENT,
  ## giving n_scenarios x n_reps independent branches dispatched in parallel.
  tar_target(
    name      = landis_run_output_rep_index,
    command   = seq_len(5L),
    iteration = "vector"
  ),
  landisutils::tar_landis(
    name      = landis_run_output,
    rep_index = landis_run_output_rep_index,
    ...
    pattern   = cross(landis_run_name, landis_run_output_rep_index)
  )
)
}\if{html}{\out{</div>}}

\strong{Caching:} each (scenario, replicate) branch is cached independently.
Adding replicates (increasing the \code{seq_len()} value) only computes new
branches; existing ones remain untouched.

The \code{method} is resolved at \emph{factory-call time} (i.e., when \verb{_targets.R} is
evaluated) and baked into the target command. This ensures that
\code{{crew}} worker processes, which do not inherit R session options, receive
the correct values. Set \code{options(landisutils.run.method = ...)} in
\verb{_local.R} before the pipeline list is constructed to control the method
per machine.
}
\seealso{
\code{\link[=landis_replicate]{landis_replicate()}}, \code{\link[=landis_run_local]{landis_run_local()}}, \code{\link[=landis_run_docker]{landis_run_docker()}}

Other LANDIS-II execution helpers:
\code{\link[=host_cpu_info]{host_cpu_info()}},
\code{\link[=landis_archive_rep]{landis_archive_rep()}},
\code{\link[=landis_find]{landis_find()}},
\code{\link[=landis_find_docker]{landis_find_docker()}},
\code{\link[=landis_pool_exec]{landis_pool_exec()}},
\code{\link[=landis_pool_restart_one]{landis_pool_restart_one()}},
\code{\link[=landis_pool_start]{landis_pool_start()}},
\code{\link[=landis_pool_stop]{landis_pool_stop()}},
\code{\link[=landis_replicate]{landis_replicate()}},
\code{\link[=landis_run_docker]{landis_run_docker()}},
\code{\link[=landis_run_local]{landis_run_local()}},
\code{\link[=read_landis_resource_logs]{read_landis_resource_logs()}},
\code{\link[=write_landis_scenario_file]{write_landis_scenario_file()}}
}
\concept{LANDIS-II execution helpers}
