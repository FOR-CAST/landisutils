% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_dynamic_fire.R
\name{build_calibration_spinup_scenario}
\alias{build_calibration_spinup_scenario}
\title{Build a calibration spinup scenario directory}
\usage{
build_calibration_spinup_scenario(
  out_dir,
  template_dir,
  duration = 1L,
  community_output_year = 0L,
  cell_length
)
}
\arguments{
\item{out_dir}{Character. Destination directory (created or overwritten).}

\item{template_dir}{Character. Existing production scenario directory to copy
from. Must contain \code{forc-succession.txt}, \code{species.txt}, \code{ecoregions.txt},
\code{ecoregions.tif}, \code{climate.txt}, \code{ForCS_DM.txt}, \code{initial-communities.csv},
\code{initial-communities.tif}, and the ForCS data CSVs.}

\item{duration}{Integer. Simulation duration in years. LANDIS-II minimum 1.}

\item{community_output_year}{Integer. Year at which the snapshot is consumed
downstream (caller chooses 0 for post-spinup state; default 0).}

\item{cell_length}{Integer. Raster cell size in metres.}
}
\value{
Character scalar: absolute path to the written \code{scenario.txt}.
}
\description{
Materialises a self-contained LANDIS-II scenario whose only purpose is to run
the succession backend (ForC Succession or Biomass Succession, auto-detected
from the template's config file) for \code{duration} years, emit a snapshot of the
spun-up cohort community via the Output Biomass Community extension, and exit.
}
\details{
The Output Biomass Community extension emits at multiples of its Timestep
starting from \strong{year 0} (post-init, pre-step), so with Timestep = 1 and
Duration = 1 we get two snapshot CSVs: \verb{community-input-file-0.csv}
(post-spinup state -- this is the one we want) and \verb{community-input-file-1.csv}
(after one year of ANPP). The TIF (\verb{output-community-0.tif}) is emitted only
once at year 0 -- cohort communities don't repartition in a no-disturbance
run, so one raster suffices for both years.

Note on CSV schema: LANDIS-II Output Biomass Community v3 writes a 5-column
file (\verb{MapCode, SpeciesName, CohortAge, CohortBiomass, CohortANPP}). LANDIS-II's
initial-communities parser tolerates the extra \code{CohortANPP} column, so the
file is drop-in usable as \code{InitialCommunitiesFiles} without post-processing.

Implementation: copy every top-level file from \code{template_dir} (a production
scenario directory), strip the disturbance stack from the copied scenario.txt,
add an Output Biomass Community extension, and rewrite scenario.txt with
Duration = \code{duration}. Rep subdirectories are NOT copied.
}
\seealso{
Other Dynamic Fire calibration helpers:
\code{\link[=apply_calibrated_hi_prop]{apply_calibrated_hi_prop()}},
\code{\link[=apply_calibrated_ignprob]{apply_calibrated_ignprob()}},
\code{\link[=bc_fuel_code_to_base]{bc_fuel_code_to_base()}},
\code{\link[=build_calibration_scenario_template]{build_calibration_scenario_template()}},
\code{\link[=calibrate_dynamic_fire]{calibrate_dynamic_fire()}},
\code{\link[=calibration_par_names]{calibration_par_names()}},
\code{\link[=default_severity_prior_sturtevant2009]{default_severity_prior_sturtevant2009()}},
\code{\link[=loss_from_stats]{loss_from_stats()}},
\code{\link[=parse_dynamic_fire_logs]{parse_dynamic_fire_logs()}},
\code{\link[=patch_fire_config]{patch_fire_config()}},
\code{\link[=run_calibration_spinup]{run_calibration_spinup()}},
\code{\link[=run_calibration_validation]{run_calibration_validation()}},
\code{\link[=save_observed_fire_targets]{save_observed_fire_targets()}},
\code{\link[=sim_landis]{sim_landis()}},
\code{\link[=sim_mock]{sim_mock()}},
\code{\link[=sim_r_reimpl]{sim_r_reimpl()}}
}
\concept{Dynamic Fire calibration helpers}
