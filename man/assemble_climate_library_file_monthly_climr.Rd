% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/climate_monthly_climr.R
\name{assemble_climate_library_file_monthly_climr}
\alias{assemble_climate_library_file_monthly_climr}
\title{Assemble LANDIS-II Climate Library wide-format table from a climr Arrow dataset}
\usage{
assemble_climate_library_file_monthly_climr(dataset_path, vars, eco_lookup)
}
\arguments{
\item{dataset_path}{character. Path to a scenario-tagged \code{Climr_Monthly}
Arrow dataset directory (containing \code{Variable=...}/\code{Year=...}/...
partitions). Typically \verb{<cache_root>/Climr_Monthly/<sa_hash>/<scenario_tag>}.}

\item{vars}{character vector of lowercase variable names (e.g.
\code{c("prcp", "tmax", "tmin")}); same set accepted by
\code{\link[=prep_monthly_weather_climr]{prep_monthly_weather_climr()}}.}

\item{eco_lookup}{\code{data.frame} with columns \code{id} (cell id, matching the
\code{id} column in the cached partitions) and \code{EcoID} (ecoregion id from the
study-area \code{id} field). Typically built by \code{.climr_build_xyz()} (internal).}
}
\value{
\code{tbl_df} with columns \code{Year}, \code{Month}, \code{Variable}, and one column
per ecoregion id.
}
\description{
Reads the partitioned Arrow CSV dataset written by
\code{\link[=get_clim_monthly_climr]{get_clim_monthly_climr()}}, joins per-cell rows to ecoregion ids via
\code{eco_lookup}, summarises by \verb{(Year, Month, EcoID, Variable)} with
\code{mean(..., na.rm = TRUE)}, applies unit conversions to match LANDIS-II
conventions, and pivots to wide format keyed by ecoregion id.
}
\details{
For projection-mode partitions (which carry extra \code{GCM} and \code{SSP} columns),
the same group-and-mean step collapses across GCMs/SSPs/cells, yielding a
single multi-GCM ensemble-mean value per \verb{(Year, Month, EcoID, Variable)}
as expected by the LANDIS-II Climate Library.

Unit conversions applied:
\itemize{
\item \code{PPT} (mm) \eqn{\to} cm (\eqn{\div 10})
}
}
\seealso{
\code{\link[=get_clim_monthly_climr]{get_clim_monthly_climr()}}, \code{\link[=prep_monthly_weather_climr]{prep_monthly_weather_climr()}},
\code{\link[=writeClimateData]{writeClimateData()}}
}
