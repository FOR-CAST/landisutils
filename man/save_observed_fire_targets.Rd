% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_dynamic_fire.R
\name{save_observed_fire_targets}
\alias{save_observed_fire_targets}
\title{Save observed fire-regime targets (NFDB-derived) for calibration loss}
\usage{
save_observed_fire_targets(
  primary_points,
  primary_polys,
  fire_years,
  fuel_types_rast,
  path,
  secondary_points = NULL,
  secondary_polys = NULL,
  primary_label = "primary",
  secondary_label = "secondary",
  fuel_code_to_base = bc_fuel_code_to_base()
)
}
\arguments{
\item{primary_points, primary_polys}{SpatVector. NFDB ignition points and
fire polygons for the primary ecoregion (the LANDIS simulation extent).}

\item{fire_years}{Integer vector. Years over which counts are normalised
(denominator for \code{lambda_obs}).}

\item{fuel_types_rast}{SpatRaster. Integer-coded fuel-type raster covering
the LANDIS simulation extent.}

\item{path}{Character. Output \code{.rds} path. Parent dir created if missing.}

\item{secondary_points, secondary_polys}{SpatVector or NULL. Same, for an
optional regional-context ecoregion. \code{area_by_fuel_ha} is NOT computed
for the secondary (see Details).}

\item{primary_label, secondary_label}{Character. Labels for the two ecoregions
(e.g., \code{"FRU59"} / \code{"FRT12"}). Stored in the payload for reproducibility.}

\item{fuel_code_to_base}{Named character vector. Mapping from
\code{fuel_types_rast} integer codes (as character names) to the five base
fuel types from \code{\link[=defaultFuelTypeTable]{defaultFuelTypeTable()}}. NA values mark non-fuel codes
to be excluded. Default: \code{\link[=bc_fuel_code_to_base]{bc_fuel_code_to_base()}}.}
}
\value{
Character. Absolute path to the written file.
}
\description{
Pre-computes per-ecoregion observed summaries that downstream calibration
loss components (\code{\link[=loss_from_stats]{loss_from_stats()}}) compare simulated fires against.
Saves a single small \code{.rds} payload of base R types -- so DEoptim workers
can read it from disk without terra/sf in their environments.
}
\details{
Loss-component consumers:
\itemize{
\item \code{L_count} uses \code{n_fires_by_year} (mean + sd for normalisation).
\item \code{L_size} uses \code{fire_sizes_ha} (sorted vector; KS test against sim).
\item \code{L_area_fuel} uses primary-ecoregion \code{area_by_fuel_ha} (Tier 2;
weight 0 in Tier 1).
\item \code{L_severity} stays NULL; populate from literature priors when Tier 2
severity matching is implemented.
}

Per ecoregion (\code{primary_ecoregion}, \code{secondary_ecoregion}):
\itemize{
\item Fire counts come from NFDB IGNITION POINTS (one row = one ignition).
NFDB polygons are sparser (only mapped for larger fires).
\item Fire sizes come from NFDB points' \code{SIZE_HA} column (zeros dropped to
keep the lognormal-flavoured size distribution positive).
\item \code{area_by_fuel_ha} is computed for the PRIMARY ecoregion only via
polygon overlay on \code{fuel_types_rast}. \code{fuel_types_rast} covers the
LANDIS simulation domain; secondary-ecoregion polygons typically
extend well beyond that extent, making a secondary computation
misleading (it would just be the primary value over again).
}
}
\seealso{
Other Dynamic Fire calibration helpers:
\code{\link[=apply_calibrated_hi_prop]{apply_calibrated_hi_prop()}},
\code{\link[=apply_calibrated_ignprob]{apply_calibrated_ignprob()}},
\code{\link[=bc_fuel_code_to_base]{bc_fuel_code_to_base()}},
\code{\link[=build_calibration_scenario_template]{build_calibration_scenario_template()}},
\code{\link[=build_calibration_spinup_scenario]{build_calibration_spinup_scenario()}},
\code{\link[=calibrate_dynamic_fire]{calibrate_dynamic_fire()}},
\code{\link[=calibration_par_names]{calibration_par_names()}},
\code{\link[=loss_from_stats]{loss_from_stats()}},
\code{\link[=parse_dynamic_fire_logs]{parse_dynamic_fire_logs()}},
\code{\link[=patch_fire_config]{patch_fire_config()}},
\code{\link[=run_calibration_spinup]{run_calibration_spinup()}},
\code{\link[=sim_landis]{sim_landis()}},
\code{\link[=sim_mock]{sim_mock()}},
\code{\link[=sim_r_reimpl]{sim_r_reimpl()}}
}
\concept{Dynamic Fire calibration helpers}
