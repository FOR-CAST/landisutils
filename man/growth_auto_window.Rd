% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/growth_calibration_score.R
\name{growth_auto_window}
\alias{growth_auto_window}
\title{Derive one species' fitting window from where its references have support}
\usage{
growth_auto_window(
  reference,
  longevity,
  age_floor = 20,
  age_quantile = 0.95,
  senescence_frac = 0.45,
  mort_shp = NULL,
  sources = c("SORTIE", "TIPSY", "VDYP")
)
}
\arguments{
\item{reference}{A data frame of reference observations, with columns
\code{source} (\code{"SORTIE"}, \code{"TIPSY"}, \code{"VDYP"} or \code{"Ground plots"}), \code{age} and
\code{aboveground_c_mg_ha}.}

\item{longevity}{Numeric. The species' \code{longevity}.}

\item{age_floor}{Numeric. Youngest age to score.}

\item{age_quantile}{Numeric. Quantile of observed plot ages to close at.}

\item{senescence_frac}{Numeric. Fraction of longevity at which to close, used
only when \code{mort_shp} is \code{NULL}.}

\item{mort_shp}{Numeric \code{MortalityCurve} for this species, or \code{NULL}. When
supplied it sets the cap and \code{senescence_frac} is ignored.}

\item{sources}{Character. Modelled reference series to consider.}
}
\value{
Numeric length-2.
}
\description{
Nobody should have to nominate an age range by hand. Both bounds are dictated
by the data and by a species attribute that is not being fitted, so both can
be read off directly.
}
\details{
The window OPENS at \code{age_floor}. Stands younger than that are essentially
unmeasured -- the ground-plot programs do not sample them -- and it is also
the range where LANDIS-II is known to overestimate biomass for reasons that
have nothing to do with these four parameters, so scoring there would import a
bias the sweep cannot fix.

The window CLOSES at the earliest of three limits: the \code{age_quantile} of the
observed plot ages, beyond which the cloud thins to a handful of stands; the
end of the modelled reference curve; and \code{senescence_frac} x \code{longevity}.
LANDIS-II ramps mortality up as a cohort approaches \code{longevity} and the curve
then falls to exactly zero and stays there, so an open-ended window scores the
modelled die-off rather than the level the stand holds.

WHERE THAT CAP BELONGS IS A PROPERTY OF \code{MortalityCurve}. The extension
defines it as a position in the lifespan (2.12.4: 5 puts onset at 10\% of life
span, 25 at 85\%), so the age at which a species leaves its plateau varies by
nearly twofold across the documented range. Supply \code{mort_shp} and the cap is
that species' own onset, via \code{\link[=growth_mortality_onset_frac]{growth_mortality_onset_frac()}}. Measured on one
calibration, the departure from 95\% of peak biomass ran 0.43-0.48 x longevity
at \code{MortalityCurve} 10, 0.63-0.70 at 15 and 0.82-0.84 at 25 -- so a single
fraction cannot separate a species that breaks up early from one that holds
its stand almost to the end, which is the distinction the parameter exists to
make.

The cap is at the ONSET of mortality, not at peak biomass, and is therefore
conservative: biomass keeps rising for a period after onset while growth still
exceeds mortality. That is deliberate. Peak location depends on \code{GrowthCurve}
as well, and \code{GrowthCurve} may still be swept -- a peak-based cap would then
score different candidates over different ranges and could not rank them
fairly. Onset depends on \code{MortalityCurve} alone, so as long as that is
assigned rather than swept, every candidate for a species sees one window.

\code{senescence_frac} remains as the fallback when \code{mort_shp} is not supplied. Its
default of 0.45 was calibrated against a parameterisation that gave every
species a \code{MortalityCurve} near 23; it does not generalise, and on a set
carrying 10s the earliest 95\%-of-peak departure falls to 0.433, below the cap
itself.
}
\seealso{
Other growth calibration helpers:
\code{\link[=extract_landscape_cohort_structures]{extract_landscape_cohort_structures()}},
\code{\link[=growth_add_objective]{growth_add_objective()}},
\code{\link[=growth_best_candidates]{growth_best_candidates()}},
\code{\link[=growth_bin_observations]{growth_bin_observations()}},
\code{\link[=growth_calibration_design]{growth_calibration_design()}},
\code{\link[=growth_calibration_partition]{growth_calibration_partition()}},
\code{\link[=growth_calibration_work_root]{growth_calibration_work_root()}},
\code{\link[=growth_climatic_distance]{growth_climatic_distance()}},
\code{\link[=growth_climatic_weight]{growth_climatic_weight()}},
\code{\link[=growth_expand_over_pseudo_species]{growth_expand_over_pseudo_species()}},
\code{\link[=growth_factorial_ratio_grid]{growth_factorial_ratio_grid()}},
\code{\link[=growth_fitting_windows]{growth_fitting_windows()}},
\code{\link[=growth_identifiability]{growth_identifiability()}},
\code{\link[=growth_inflation_factor]{growth_inflation_factor()}},
\code{\link[=growth_mortality_onset_frac]{growth_mortality_onset_frac()}},
\code{\link[=growth_plot_palette]{growth_plot_palette()}},
\code{\link[=growth_pseudo_species_name]{growth_pseudo_species_name()}},
\code{\link[=growth_reference_curves]{growth_reference_curves()}},
\code{\link[=growth_reference_inflection]{growth_reference_inflection()}},
\code{\link[=growth_score_fit]{growth_score_fit()}},
\code{\link[=growth_scoring_for]{growth_scoring_for()}},
\code{\link[=growth_smooth_observations]{growth_smooth_observations()}},
\code{\link[=growth_structure_cell_curves]{growth_structure_cell_curves()}},
\code{\link[=growth_structure_cohort_table]{growth_structure_cohort_table()}},
\code{\link[=growth_structure_design]{growth_structure_design()}},
\code{\link[=growth_structure_summary]{growth_structure_summary()}},
\code{\link[=growth_window_for]{growth_window_for()}},
\code{\link[=plot_growth_calibration]{plot_growth_calibration()}},
\code{\link[=plot_growth_candidate]{plot_growth_candidate()}},
\code{\link[=plot_growth_factorial_sensitivity]{plot_growth_factorial_sensitivity()}},
\code{\link[=plot_growth_structures]{plot_growth_structures()}},
\code{\link[=read_growth_scoring]{read_growth_scoring()}},
\code{\link[=read_landscape_cohort_structures]{read_landscape_cohort_structures()}},
\code{\link[=scale_linetype_growth_reference]{scale_linetype_growth_reference()}},
\code{\link[=write_growth_review_bundle]{write_growth_review_bundle()}}
}
\concept{growth calibration helpers}
