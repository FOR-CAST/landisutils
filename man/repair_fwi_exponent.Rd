% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/climate_daily_biosim.R
\name{repair_fwi_exponent}
\alias{repair_fwi_exponent}
\title{Repair BioSIM's sign-dropped scientific-notation exponents}
\usage{
repair_fwi_exponent(v)
}
\arguments{
\item{v}{numeric vector of a single FWI System code.}
}
\value{
\code{v} with artifact values restored; all other values untouched.
}
\description{
\code{BioSIM} returns the Fire Weather Index System codes and indices as text, and drops
the minus sign from the exponent whenever a value is small enough to be formatted in
scientific notation (below \code{1e-4}). A true \code{6.49936E-05} comes back as \code{6.49936E+05}:
the mantissa is intact and only the exponent's sign is lost, so
\code{v * 10^(-2 * floor(log10(v)))} recovers the original value exactly. Verified against
the FWI System equations -- \verb{FFMC = 17.1624, WS = 8.96132} gives \code{ISI = 6.49934e-05},
and \code{BioSIM} returned \code{649936}.
}
\details{
Only saturated fuels drive the codes below \code{1e-4}, so the artifact is confined to wet,
low-hazard records, but it inflates them by up to eighteen orders of magnitude. Any
downstream mean over cells or days is destroyed by a handful of contaminated records,
so this must be applied before the values are aggregated or used to derive indices.

Applied to the three moisture codes (\code{FFMC}, \code{DMC}, \code{DC}), which \code{BioSIM} reports
directly and which cannot be recomputed from the other returned columns without a
precipitation series. The behaviour indices (\code{ISI}, \code{BUI}, \code{FWI}) are pure functions of
those codes and are re-derived from the repaired values by \code{\link[=get_fwi_daily]{get_fwi_daily()}} instead.
}
\seealso{
\code{\link[=get_fwi_daily]{get_fwi_daily()}}
}
