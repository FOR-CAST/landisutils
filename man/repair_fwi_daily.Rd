% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/climate_daily_biosim.R
\name{repair_fwi_daily}
\alias{repair_fwi_daily}
\title{Repair and re-derive a table of BioSIM \code{FWI_Daily} records}
\usage{
repair_fwi_daily(fwi_daily, validate = TRUE)
}
\arguments{
\item{fwi_daily}{data frame of \code{FWI_Daily} records with at least \code{FFMC}, \code{DMC}, \code{DC}
and \code{WS}.}

\item{validate}{logical. Check the repaired values against physical bounds and error
on a violation. Default \code{TRUE}; a violation means a failure mode the repair does not
characterise, which must not reach a weather database silently.}
}
\value{
\code{fwi_daily} with \code{FFMC}, \code{DMC} and \code{DC} repaired and \code{BUI}, \code{ISI} and \code{FWI}
re-derived.
}
\description{
The single entry point for making BioSIM \code{FWI_Daily} output trustworthy. Used by
\code{\link[=get_fwi_daily]{get_fwi_daily()}}, and exported so that projects with their own BioSIM fetch apply
exactly the same correction rather than reimplementing it.
}
\details{
Two steps:
\enumerate{
\item \strong{Repair the three moisture codes} \code{FFMC}, \code{DMC} and \code{DC} in place, via
\code{\link[=repair_fwi_exponent]{repair_fwi_exponent()}}. These are recursive, precipitation-driven daily codes, and
\code{FWI_Daily} does not return precipitation in every configuration, so they cannot in
general be recomputed from the other columns and must be recovered as they stand.
Repairing \code{DMC} and \code{DC} matters as much as \code{FFMC}: a corrupt \code{DMC} otherwise flows
into the buildup index and from there into \code{FWI}.
\item \strong{Discard and re-derive the three behaviour indices} \code{BUI}, \code{ISI} and \code{FWI} from
the repaired codes using \code{cffdrs}. These carry the same artifact as the codes --
\code{BUI} drops below \code{1e-4} whenever \code{DMC} does -- but they are pure functions of the
codes, so recomputing is strictly safer than repairing: it needs no threshold and
cannot mistake a genuine extreme for corruption. Nothing is lost by doing so. On
uncorrupted records the recomputed values reproduce BioSIM's own to within \code{5e-6}
relative for \code{BUI} and 0.8\% for \code{ISI} and \code{FWI}.
}

Records BioSIM did not model (\code{-9999} sentinels, which callers map to \code{NA} before
calling) pass through as \code{NA}.
}
\seealso{
\code{\link[=repair_fwi_exponent]{repair_fwi_exponent()}}, \code{\link[=get_fwi_daily]{get_fwi_daily()}}
}
