% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landis_results.R
\name{read_landis_raster}
\alias{read_landis_raster}
\title{Read a LANDIS-II output raster in the row order LANDIS-II wrote it}
\usage{
read_landis_raster(path, template = NULL)
}
\arguments{
\item{path}{A character path to a LANDIS-II output GeoTIFF.}

\item{template}{Optional \code{terra::SpatRaster}, or a character path to the
study area rasterToMatch. When supplied, its CRS and extent are copied
onto the result.}
}
\value{
A \code{terra::SpatRaster} whose row 1 is the first row LANDIS-II wrote,
i.e. the first row of the input maps.
}
\description{
\strong{Always open a LANDIS-II output map with this rather than with
\code{\link[terra:rast]{terra::rast()}}.} LANDIS-II writes its GeoTIFFs with no geotransform, which
GDAL reports as a south-up raster; \code{terra::rast()} normalises that by
reversing the rows, so the raster it returns is a vertical mirror of the
landscape LANDIS-II simulated. Nothing about the mirrored raster looks wrong
-- it has the right dimensions and the right values -- so the error surfaces
only as maps and per-region summaries that disagree with the inputs.
}
\details{
This reads the file, restores the written row order when the file is stored
south-up (which is checked per file, not assumed), and optionally
georeferences the result against a template via \code{\link[=georef_landis_raster]{georef_landis_raster()}}.
}
\examples{
\dontrun{
sev <- read_landis_raster("fire/severity-10.tif", "rasterToMatch_Chine.tif")
}
}
\seealso{
Other output-reading helpers:
\code{\link[=georef_landis_raster]{georef_landis_raster()}}
}
\concept{output-reading helpers}
