% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_transitions.R
\name{read_biomass_c_snapshots_for_scenario}
\alias{read_biomass_c_snapshots_for_scenario}
\title{Open + collect a per-scenario biomass_snapshots Arrow dataset}
\usage{
read_biomass_c_snapshots_for_scenario(
  scenario_dir,
  subdir = "_aggregates/biomass_snapshots"
)
}
\arguments{
\item{scenario_dir}{Path to a scenario's root directory (e.g. \code{"LANDIS-II/ForCS_only"}).}

\item{subdir}{Path within \code{scenario_dir} to the parquet dataset root
(default \code{"_aggregates/biomass_snapshots"}).}
}
\value{
A tibble with columns
\verb{scenario, replicate, Time, row, column, ecoregion, species, biomass}
(matching \code{\link[=read_biomass_c_snapshots]{read_biomass_c_snapshots()}}'s output), or \code{NULL}.
}
\description{
Convenience wrapper that opens \verb{<scenario_dir>/<subdir>} as an Arrow dataset
partitioned by \code{replicate}, collects the full contents into a tibble, and
returns \code{NULL} for missing directories or empty datasets so callers can
short-circuit gracefully.
}
\details{
Isolated from the rest of the \code{\link[=read_biomass_c_snapshots]{read_biomass_c_snapshots()}} pipeline so its
body stays stable across project-side refactors: \code{{targets}} does not track
installed-package function bodies, so consuming targets are not invalidated
by cosmetic changes to how the collect happens under the hood.  Callers that
want that invalidation guarantee should call this function from their target
commands rather than inlining the \code{arrow::open_dataset() |> collect()} chain.

The default \code{subdir = "_aggregates/biomass_snapshots"} matches the layout
produced by \code{\link[=write_biomass_c_snapshots_parquet]{write_biomass_c_snapshots_parquet()}}-style writers in the
FOR-CAST post-processing pipeline; override for other layouts.
}
\seealso{
Other Vegetation transition helpers:
\code{\link[=biomass_landscape_summary]{biomass_landscape_summary()}},
\code{\link[=community_label]{community_label()}},
\code{\link[=leading_species]{leading_species()}},
\code{\link[=plot_species_biomass]{plot_species_biomass()}},
\code{\link[=plot_transitions]{plot_transitions()}},
\code{\link[=read_biomass_c_snapshots]{read_biomass_c_snapshots()}},
\code{\link[=read_biomass_output_rasters]{read_biomass_output_rasters()}},
\code{\link[=transition_data]{transition_data()}}
}
\concept{Vegetation transition helpers}
