% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landis_pool.R
\name{landis_pool_start}
\alias{landis_pool_start}
\title{Start a warm Docker pool for LANDIS-II calibration}
\usage{
landis_pool_start(
  n,
  image = NULL,
  scratch_root,
  cpu_limit = 4,
  mem_limit = "8g",
  pull = FALSE,
  name_prefix = NULL
)
}
\arguments{
\item{n}{Integer. Number of pool containers to start. Typically the number
of parallel DEoptim workers.}

\item{image}{Character or NULL. Docker image reference. NULL =
\code{getOption("landisutils.docker.image")}.}

\item{scratch_root}{Character. Host directory bind-mounted to \verb{/scratch}
inside every container. Must be a stable absolute path. All per-trial
directories used by \code{\link[=landis_pool_exec]{landis_pool_exec()}} must be SUBDIRECTORIES of this.}

\item{cpu_limit}{Numeric or NULL. Per-container \code{--cpus} cap. Default 4.}

\item{mem_limit}{Character or NULL. Per-container \code{--memory} cap. Default \code{"8g"}.}

\item{pull}{Logical. When TRUE, \verb{docker pull} the image before starting.}

\item{name_prefix}{Character or NULL. Optional prefix for the container name;
the rest is auto-generated to avoid collisions across concurrent pools.}
}
\value{
A list with \code{names} (container names), \code{image}, \code{scratch_root}, \code{n},
\code{started_at}, and \code{digest} (image RepoDigest if available).
}
\description{
Spawns \code{n} detached containers from \code{image}, each bind-mounting \code{scratch_root}
to \verb{/scratch} inside. Each container runs a \code{sleep}-based busy loop that
responds to \verb{docker stop} by exiting cleanly. The intended workflow is to
dispatch many \code{landis_pool_exec()} calls to the pool, then call
\code{landis_pool_stop()} to tear it down -- typically via \code{on.exit()} in the
calling driver.
}
\seealso{
\code{\link[=landis_pool_exec]{landis_pool_exec()}}, \code{\link[=landis_pool_stop]{landis_pool_stop()}}, \code{\link[=landis_run_docker]{landis_run_docker()}}

Other LANDIS-II execution helpers:
\code{\link[=host_cpu_info]{host_cpu_info()}},
\code{\link[=landis_find]{landis_find()}},
\code{\link[=landis_find_docker]{landis_find_docker()}},
\code{\link[=landis_pool_exec]{landis_pool_exec()}},
\code{\link[=landis_pool_stop]{landis_pool_stop()}},
\code{\link[=landis_replicate]{landis_replicate()}},
\code{\link[=landis_run_docker]{landis_run_docker()}},
\code{\link[=landis_run_local]{landis_run_local()}},
\code{\link[=read_landis_resource_logs]{read_landis_resource_logs()}},
\code{\link[=tar_landis]{tar_landis()}},
\code{\link[=write_landis_scenario_file]{write_landis_scenario_file()}}
}
\concept{LANDIS-II execution helpers}
