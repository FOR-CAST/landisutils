% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forcs_output.R
\name{read_forcs_log_summary}
\alias{read_forcs_log_summary}
\title{Read ForCS \code{log_Summary.csv} files for one scenario (all replicates)}
\usage{
read_forcs_log_summary(paths, run_name = NULL, cell_mask = NULL)
}
\arguments{
\item{paths}{Character vector of \code{log_Summary.csv} file paths for one scenario.}

\item{run_name}{Relative scenario directory path (e.g. \code{"LANDIS-II/ForCS_only"});
when \code{NULL} (default) the scenario label is derived from the path.}

\item{cell_mask}{Optional \code{data.frame} with integer \code{row}/\code{column} columns
identifying cells in the BUFFERED simulation grid that correspond to the core
study area; when provided, only those cells are retained. Derive \code{row}/\code{column}
by spatially intersecting the buffered initial-communities raster with the core
study-area boundary (the buffered and core grids index the same physical cell
differently).}
}
\value{
A tibble combining all replicates with leading \code{scenario}/\code{replicate}
columns, or an empty tibble when \code{paths} is empty.
}
\description{
Reads one or more \code{log_Summary.csv} files and attaches \code{scenario}/\code{replicate}
labels derived from the directory structure (\verb{<scenario>/<replicate>/log_Summary.csv}),
optionally masking to the core study area.
}
\seealso{
Other ForCS output helpers:
\code{\link[=open_forcs_log_summary_dataset]{open_forcs_log_summary_dataset()}},
\code{\link[=write_forcs_log_summary_parquet]{write_forcs_log_summary_parquet()}}
}
\concept{ForCS output helpers}
