% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_dynamic_fire.R
\name{.patch_forcs_for_calibration}
\alias{.patch_forcs_for_calibration}
\title{Patch a ForC Succession config for calibration use (internal)}
\usage{
.patch_forcs_for_calibration(path, sim_years)
}
\arguments{
\item{path}{Character. Absolute path to \code{forc-succession.txt}.}

\item{sim_years}{Integer. Calibration sim duration (years).}
}
\value{
The patched lines, invisibly (also written to \code{path}).
}
\description{
Two surgical replacements in \code{forc-succession.txt}:
\itemize{
\item \verb{Timestep N} -> \verb{Timestep <sim_years + 1>}. Makes ForCS skip its
succession step for the duration of the calibration run (no growth,
no establishment, no mortality) so each DEoptim trial is purely a
fire-on-fixed-landscape experiment.
\item \code{SpinUp} data row -> \verb{0  0  1  20}. The calibration IC is already
post-spinup (from the snapshot of \code{\link[=build_calibration_spinup_scenario]{build_calibration_spinup_scenario()}}),
so per-trial spinup would be wasted compute. DOM equilibration is
also skipped -- fire severity depends on cohort biomass, not DOM.
}
}
\details{
Patches the file in place. Caller is expected to pass the calibration
scenario's own copy of \code{forc-succession.txt} (not the production one).
}
\keyword{internal}
