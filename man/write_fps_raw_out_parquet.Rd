% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fps_output.R
\name{write_fps_raw_out_parquet}
\alias{write_fps_raw_out_parquet}
\title{Write one replicate's FPSM raw output to a partitioned parquet}
\usage{
write_fps_raw_out_parquet(
  src_path,
  scenario_dir = NULL,
  subdir = "_aggregates/fps_raw_out",
  staging_dir = NULL
)
}
\arguments{
\item{src_path}{Path to one replicate's \code{FPS_raw_out.csv}.}

\item{scenario_dir}{Scenario directory to publish under. When \code{NULL} (default)
it is derived from \code{src_path}, skipping an \verb{fps/} working directory.}

\item{subdir}{Path within the scenario directory for the dataset root.}

\item{staging_dir}{Optional directory for the temporary parquet; \code{NULL}
(default) stages in the destination so the move is an atomic rename.}
}
\value{
The written parquet path.
}
\description{
The writer counterpart to \code{\link[=open_fps_raw_out_dataset]{open_fps_raw_out_dataset()}}, mirroring
\code{\link[=write_forcs_log_summary_parquet]{write_forcs_log_summary_parquet()}} including its atomic publish: the parquet
is written to a temporary file and then \code{\link[fs:file_move]{fs::file_move()}}d into place, so a
concurrent reader or a retried write never sees a partial file. \code{scenario} is
embedded as a data column so several per-scenario roots can be unioned.
}
\seealso{
Other FPSM helpers:
\code{\link[=fps_output_files]{fps_output_files()}},
\code{\link[=fps_pools]{fps_pools()}},
\code{\link[=fps_run_docker]{fps_run_docker()}},
\code{\link[=fps_stocks_by_pool]{fps_stocks_by_pool()}},
\code{\link[=open_fps_raw_out_dataset]{open_fps_raw_out_dataset()}},
\code{\link[=read_fps_raw_out]{read_fps_raw_out()}}
}
\concept{FPSM helpers}
