% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_transitions.R
\name{write_biomass_c_snapshots_parquet}
\alias{write_biomass_c_snapshots_parquet}
\title{Write one replicate's biomass_snapshots to a partitioned parquet}
\usage{
write_biomass_c_snapshots_parquet(
  src_path,
  times,
  cell_mask = NULL,
  subdir = "_aggregates/biomass_snapshots",
  staging_dir = NULL
)
}
\arguments{
\item{src_path}{Path to one replicate's \code{log_BiomassC.csv}.}

\item{times}{Integer vector of snapshot years (see \code{\link[=read_biomass_c_snapshots]{read_biomass_c_snapshots()}}).}

\item{cell_mask}{Optional \code{data.frame} with \code{row}/\code{column} columns identifying
the core-area cells to retain (see \code{\link[=read_biomass_c_snapshots]{read_biomass_c_snapshots()}}).}

\item{subdir}{Path within the scenario directory for the dataset root (default
\code{"_aggregates/biomass_snapshots"}, matching
\code{\link[=read_biomass_c_snapshots_for_scenario]{read_biomass_c_snapshots_for_scenario()}}).}

\item{staging_dir}{Optional directory for the temporary parquet before it is
moved into place; \code{NULL} (default) stages in the destination directory
(same-filesystem atomic rename).}
}
\value{
The written parquet path.
}
\description{
Reads a single replicate's \code{log_BiomassC.csv} via \code{\link[=read_biomass_c_snapshots]{read_biomass_c_snapshots()}}
(Arrow-filtered to \code{times}, masked to \code{cell_mask}, cohort ages summed) and
writes it to
\verb{<scenario_dir>/<subdir>/replicate=<rep>/part-0.parquet}, where
\code{scenario_dir} is inferred as \code{dirname(dirname(src_path))} and \verb{<rep>} is the
replicate directory. This is the writer counterpart to
\code{\link[=read_biomass_c_snapshots_for_scenario]{read_biomass_c_snapshots_for_scenario()}}; colocating the parquet inside the
scenario directory means it is archived alongside the raw replicate outputs
and adding a replicate is a single write with no downstream ripple.
}
\details{
The publish is atomic: the parquet is written to a temporary file and then
\code{\link[fs:file_move]{fs::file_move()}}d into place, so a concurrent reader or a retried write never
observes a partial file -- safe for many replicate writers running at once
against an NFS output directory. When \code{staging_dir} is supplied the temporary
is written there (e.g. per-host scratch, keeping the interim bytes off NFS)
and moved cross-filesystem; the default stages in the destination directory so
the move is a same-filesystem atomic rename.
}
\seealso{
Other Vegetation transition helpers:
\code{\link[=biomass_landscape_summary]{biomass_landscape_summary()}},
\code{\link[=community_label]{community_label()}},
\code{\link[=leading_species]{leading_species()}},
\code{\link[=plot_species_biomass]{plot_species_biomass()}},
\code{\link[=plot_transitions]{plot_transitions()}},
\code{\link[=read_biomass_c_snapshots]{read_biomass_c_snapshots()}},
\code{\link[=read_biomass_c_snapshots_for_scenario]{read_biomass_c_snapshots_for_scenario()}},
\code{\link[=read_biomass_output_rasters]{read_biomass_output_rasters()}},
\code{\link[=transition_data]{transition_data()}}
}
\concept{Vegetation transition helpers}
