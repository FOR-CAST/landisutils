% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_scenario.R
\name{validate_landis_scenario}
\alias{validate_landis_scenario}
\title{Validate a generated LANDIS-II scenario directory before running it}
\usage{
validate_landis_scenario(
  path,
  scenario_file = "scenario.txt",
  error = TRUE,
  max_ic_csv_mb = 200
)
}
\arguments{
\item{path}{Character. Path to the assembled scenario directory.}

\item{scenario_file}{Character. Name of the master scenario file within
\code{path}. Defaults to \code{"scenario.txt"}.}

\item{error}{Logical. When \code{TRUE} (default), stop with every problem found.
When \code{FALSE}, return them instead -- use this to survey scenarios without
failing, e.g. when introducing a new check.}

\item{max_ic_csv_mb}{Numeric. Size above which an initial-communities CSV is
reported. The LANDIS-II parser builds one \code{ExpandoObject} per row and costs
a large multiple of the file size, so a per-pixel (undeduplicated) snapshot
aborts with \code{System.OutOfMemoryException} before the simulation starts.}
}
\value{
Invisibly, a character vector of problems -- empty when the scenario
is clean. With \code{error = TRUE} a non-empty result is raised instead.
}
\description{
Checks a fully assembled scenario directory for the input defects that
LANDIS-II either reports unhelpfully or does not report at all. It is a pure-R
guard: no Docker, no simulation. Run it once per scenario, not once per
replicate -- replicates copy an already-validated directory.
}
\details{
Checks performed:

\itemize{
\item \strong{Existence and non-emptiness} of every input file referenced by
\code{scenario.txt} and by each extension configuration it names. Files
LANDIS-II will \emph{write} are excluded, via \code{output_manifest.txt} and an
internal list of output-naming directives.
\item \strong{Pixel type} of every map: LANDIS-II opens rasters through
\code{Landis.RasterIO.Gdal.GdalInputRaster.NewInputBand}, which accepts only
GDAL \code{Byte}, \code{Int16}, \code{Int32}, \code{Float32} and \code{Float64}. See
\code{\link[=landis_datatype]{landis_datatype()}}.
\item \strong{Dimensions}: every map must match the ecoregions map.
\item \strong{Orientation}: a map stored in the wrong row order relative to the
ecoregions map is detected by comparing per-cell mask agreement against
the agreement its vertically flipped self would achieve. See below.
\item \strong{Initial-communities integrity}: every map code present in the
raster resolves to rows in the initial-communities CSV, allowing the one
deliberately row-less empty-community code that
\code{\link[=dedup_community_snapshot]{dedup_community_snapshot()}} creates; and the CSV is small enough that the
LANDIS-II parser will not exhaust the container's memory reading it.
}
}
\section{Detecting a mirrored map}{


A map written in the wrong row order is the most dangerous defect in this set,
because nothing rejects it: dimensions, values and totals are all correct, and
the run completes with the vegetation displaced relative to the ecoregion,
fire-region and topography maps. It cost a 25-generation Dynamic Fire
calibration.

Orientation metadata cannot catch it. The mirrored map is written back by
\code{terra} and is north-up; only its \emph{content} is reversed. So the check compares
content: for each code map, the fraction of cells whose active/inactive state
matches the ecoregions map, against the same fraction for the map's flipped
self. A correctly oriented map scores higher as-is; a mirrored one scores
higher flipped, and flipping swaps the pair exactly. No absolute threshold is
involved, which is what makes this robust: it is two measurements of the same
landscape rather than a tuned constant.

Measured on the two assembled BC_HRV scenarios, as-is versus flipped:
initial communities 0.9720/0.7605 and 0.9798/0.4945; fire ecoregions
1.0000/0.7799 and 1.0000/0.4940.

Note this is deliberately NOT the stricter "every active cell carries a map
code". Measured on those same working scenarios, 10,897 and 95,063 active
ecoregion cells carry no initial-communities code -- they are cells with no
cohorts, which Biomass Succession handles -- so the strict form would reject
valid production input.

Maps read through \code{\link[=read_landis_raster]{read_landis_raster()}}, never \code{terra::rast()}, so the
comparison is made in the row order LANDIS-II itself will read.
}

\examples{
\dontrun{
validate_landis_scenario("LANDIS-II/hrv_biomass_fire/Chine")

## survey without failing, when introducing a new check
validate_landis_scenario(scenario_dir, error = FALSE)
}
}
\seealso{
\code{\link[=scenario]{scenario()}}, \code{\link[=write_landis_scenario_file]{write_landis_scenario_file()}}, \code{\link[=landis_datatype]{landis_datatype()}},
\code{\link[=read_landis_raster]{read_landis_raster()}}, \code{\link[=dedup_community_snapshot]{dedup_community_snapshot()}}

Other LANDIS-II execution helpers:
\code{\link[=host_cpu_info]{host_cpu_info()}},
\code{\link[=landis_archive_rep]{landis_archive_rep()}},
\code{\link[=landis_assert_version]{landis_assert_version()}},
\code{\link[=landis_find]{landis_find()}},
\code{\link[=landis_find_docker]{landis_find_docker()}},
\code{\link[=landis_pool_exec]{landis_pool_exec()}},
\code{\link[=landis_pool_restart_one]{landis_pool_restart_one()}},
\code{\link[=landis_pool_start]{landis_pool_start()}},
\code{\link[=landis_pool_stop]{landis_pool_stop()}},
\code{\link[=landis_replicate]{landis_replicate()}},
\code{\link[=landis_run_docker]{landis_run_docker()}},
\code{\link[=landis_run_local]{landis_run_local()}},
\code{\link[=landis_target_version]{landis_target_version()}},
\code{\link[=landis_version]{landis_version()}},
\code{\link[=read_landis_resource_logs]{read_landis_resource_logs()}},
\code{\link[=tar_landis]{tar_landis()}},
\code{\link[=write_landis_scenario_file]{write_landis_scenario_file()}}
}
\concept{LANDIS-II execution helpers}
