% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_growth_curves.R
\name{plot_species_growth_curves}
\alias{plot_species_growth_curves}
\title{Plot fitted LANDIS-II growth curves against PSP observations}
\usage{
plot_species_growth_curves(
  landis_curves,
  psp_points,
  biomass_label = quote("Biomass" ~ (g ~ m^-2))
)
}
\arguments{
\item{landis_curves}{A data frame with columns \code{species}, \code{standAge}, and
\code{BscaledNonLinear} -- e.g. the \code{speciesGrowthCurvesLandis} output of
\code{Biomass_speciesParameters} run in LANDIS mode.}

\item{psp_points}{A data frame with columns \code{speciesTemp} (species code),
\code{standAge}, \code{biomass}, and \code{ecoregion} -- e.g. the \code{speciesGrowthCurvesPSP}
output of \code{Biomass_speciesParameters} run in LANDIS mode.}

\item{biomass_label}{Axis label for biomass, as a plotmath expression (or a
plain string). Defaults to \code{quote("Biomass" ~ (g ~ m^-2))}.}
}
\value{
A \code{ggplot} object.
}
\description{
Overlays the fitted LANDIS-version growth curve (\code{BscaledNonLinear}) on the
permanent-sample-plot (PSP) biomass observations behind it, faceted by
species and coloured by ecoregion. A calibration diagnostic for the Biomass
Succession species parameterization produced -- in LANDIS mode -- by the
\code{Biomass_speciesParameters} SpaDES module: it shows how each fitted growth
curve sits within the spread of the PSP data, and how that data is
distributed across ecoregions.
}
\details{
Only species present in \code{landis_curves} get a fitted line; species that have
PSP observations but no fitted curve (e.g. data-poor species, or a lumped
\code{"Other"} group) are still shown as points.
}
