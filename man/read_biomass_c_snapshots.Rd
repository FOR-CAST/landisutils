% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_transitions.R
\name{read_biomass_c_snapshots}
\alias{read_biomass_c_snapshots}
\title{Read ForCS log_BiomassC snapshots}
\usage{
read_biomass_c_snapshots(paths, times, run_name = NULL, cell_mask = NULL)
}
\arguments{
\item{paths}{Character vector of paths to \code{log_BiomassC.csv} files (one per replicate).
Each file must contain the ForCS per-cohort columns:
\verb{Time, row, column, ecoregion, species, Age, Wood, Leaf, CrsRoot, FineRoot}.}

\item{times}{Integer vector of snapshot years to extract.}

\item{run_name}{Character. Scenario directory label attached as a \code{scenario} column.
If \code{NULL} (default), \code{scenario} is set to \code{basename(dirname(dirname(paths[1])))}.}

\item{cell_mask}{\code{data.frame} or \code{NULL}. If provided, must have integer columns \code{row}
and \code{column} identifying the cells to \strong{retain} (all others are dropped).
Use this to restrict results to the core study area when the simulation was
run over a larger buffered extent.  The \code{row}/\code{column} values must correspond
to 1-indexed raster coordinates within the \strong{buffered} simulation grid, not a
separately-indexed core grid (the two grids index the same physical cells
differently).  Derive the mask via spatial intersection of the buffered Initial Communities
raster with the core study area boundary, then extract raster row/column.}
}
\value{
A \code{data.table} with columns
\verb{scenario, replicate, Time, row, column, ecoregion, species, biomass}
where \code{biomass} is total live biomass in Mg C ha^-1.
This is the canonical format accepted by all downstream functions
(\code{\link[=leading_species]{leading_species()}}, \code{\link[=community_label]{community_label()}}, \code{\link[=biomass_landscape_summary]{biomass_landscape_summary()}}).
}
\description{
\strong{ForCS-specific fast path.} Reads one or more \code{log_BiomassC.csv} files
(one per replicate) and returns per-cell, per-species total biomass at the
requested snapshot years.
Uses \code{awk} to pre-filter the files before parsing, keeping memory usage
manageable for large (multi-GB) CSV files.
}
\details{
This function avoids the LANDIS-II raster-output overhead because ForCS writes
\code{log_BiomassC.csv} unconditionally; no additional output extension is required.
For scenarios using a succession extension other than ForCS, use
\code{\link[=read_biomass_output_rasters]{read_biomass_output_rasters()}} instead.

Biomass columns (\code{Wood}, \code{Leaf}, \code{CrsRoot}, \code{FineRoot}) are summed over all
age cohorts of each species per cell, then converted from g C m^-2 to Mg C ha^-1
(multiply by 0.01).
}
\seealso{
\code{\link[=read_biomass_output_rasters]{read_biomass_output_rasters()}} for the succession-agnostic alternative.

Other Vegetation transition helpers:
\code{\link[=biomass_landscape_summary]{biomass_landscape_summary()}},
\code{\link[=community_label]{community_label()}},
\code{\link[=leading_species]{leading_species()}},
\code{\link[=plot_species_biomass]{plot_species_biomass()}},
\code{\link[=plot_transitions]{plot_transitions()}},
\code{\link[=read_biomass_output_rasters]{read_biomass_output_rasters()}},
\code{\link[=transition_data]{transition_data()}}
}
\concept{Vegetation transition helpers}
