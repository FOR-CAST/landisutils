% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_dynamic_fire.R
\name{calibrate_dynamic_fire}
\alias{calibrate_dynamic_fire}
\title{DEoptim driver for Dynamic Fire calibration}
\usage{
calibrate_dynamic_fire(observed_targets_path, scenario_template, cfg, out_dir)
}
\arguments{
\item{observed_targets_path}{Character. Path to the \code{.rds} from
\code{\link[=save_observed_fire_targets]{save_observed_fire_targets()}}.}

\item{scenario_template}{Character. Path to the calibration scenario's
\code{scenario.txt} (the return of \code{\link[=build_calibration_scenario_template]{build_calibration_scenario_template()}}).}

\item{cfg}{List. Calibration config. Expected keys:
\describe{
\item{lower, upper}{Named numeric vectors keyed by \code{\link[=calibration_par_names]{calibration_par_names()}}.}
\item{NP, itermax, strategy}{DEoptim control args.}
\item{n_reps, sim_years, weights, base_seed}{Per-trial settings.}
\item{n_cores, parallel}{Parallelism settings.}
\item{simulator}{\code{"landis"} (default), \code{"r_reimpl"}, or \code{"mock"}.}
\item{method}{\code{"docker"} (default) or \code{"local"}.}
\item{image, cpu_limit, mem_limit, pull}{Pool settings (Docker only).}
}}

\item{out_dir}{Character. Where to write the DEoptim trace + scratch
sub-directory. Created if missing.}
}
\value{
List with \code{best_params} (named numeric), \code{objective} (scalar),
\code{deoptim} (full DEoptim return), \code{trace_path} (CSV path), \code{cfg} (echo),
\code{pool_image} / \code{pool_digest} (provenance; NA when no pool was started).
}
\description{
Sets up a warm Docker pool (for the \code{landis} simulator on Docker) and a FORK
cluster of \code{n_cores} workers, then invokes \code{\link[DEoptim:DEoptim]{DEoptim::DEoptim()}} with the
multi-component loss as the objective. Pool + cluster are torn down via
\code{on.exit()} regardless of success / error / interrupt.
}
\details{
Designed to be called from a \code{tar_target} with \code{deployment = "main"} so the
outer \code{targets} crew doesn't try to dispatch this as a single worker while
it manages its own internal cluster.

Per-worker container assignment: each FORK worker sets its
\code{LANDIS_POOL_CONTAINER_IDX} env var to its 1-based pool index.
\code{\link[=sim_landis]{sim_landis()}} reads this when running inside the worker.

DEoptim is gated on \code{requireNamespace("DEoptim")}; install via
\code{renv::install("DEoptim")} before calling.
}
\seealso{
Other Dynamic Fire calibration helpers:
\code{\link[=apply_calibrated_hi_prop]{apply_calibrated_hi_prop()}},
\code{\link[=apply_calibrated_ignprob]{apply_calibrated_ignprob()}},
\code{\link[=bc_fuel_code_to_base]{bc_fuel_code_to_base()}},
\code{\link[=build_calibration_scenario_template]{build_calibration_scenario_template()}},
\code{\link[=build_calibration_spinup_scenario]{build_calibration_spinup_scenario()}},
\code{\link[=calibration_par_names]{calibration_par_names()}},
\code{\link[=loss_from_stats]{loss_from_stats()}},
\code{\link[=parse_dynamic_fire_logs]{parse_dynamic_fire_logs()}},
\code{\link[=patch_fire_config]{patch_fire_config()}},
\code{\link[=run_calibration_spinup]{run_calibration_spinup()}},
\code{\link[=save_observed_fire_targets]{save_observed_fire_targets()}},
\code{\link[=sim_landis]{sim_landis()}},
\code{\link[=sim_mock]{sim_mock()}},
\code{\link[=sim_r_reimpl]{sim_r_reimpl()}}
}
\concept{Dynamic Fire calibration helpers}
