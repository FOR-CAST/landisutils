% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/growth_calibration_plot.R
\name{plot_growth_candidate}
\alias{plot_growth_candidate}
\title{Plot the current parameter set against a candidate, for review}
\usage{
plot_growth_candidate(
  species,
  current_curve,
  candidate_curve,
  reference,
  binned = NULL,
  smooth = NULL,
  current_label = "current parameters",
  candidate_label = "best candidate",
  x_max = NULL,
  mature_window = c(100L, Inf),
  subtitle = NULL,
  density = FALSE,
  density_bins = 34L,
  density_points_max = 150L
)
}
\arguments{
\item{species}{Character. Modelled species code.}

\item{current_curve, candidate_curve}{Tibbles with \code{age} and
\code{aboveground_c_mg_ha}. \code{candidate_curve} may be \code{NULL}.}

\item{reference}{A data frame of reference observations, with columns
\code{source} (\code{"SORTIE"}, \code{"TIPSY"}, \code{"VDYP"} or \code{"Ground plots"}), \code{age} and
\code{aboveground_c_mg_ha}.}

\item{binned}{Optional tibble from \code{\link[=growth_bin_observations]{growth_bin_observations()}}. An \code{n} column,
when present, sizes the points.}

\item{smooth}{Optional tibble from \code{\link[=growth_smooth_observations]{growth_smooth_observations()}}, drawn as a
fitted line and confidence band. Display only; nothing is scored against it.}

\item{current_label, candidate_label}{Character legend labels.}

\item{x_max}{Numeric or \code{NULL}. Upper age limit. \code{NULL} (the default)
extends to the last age present in the data, so a longer run is never
silently clipped.}

\item{mature_window}{Numeric length-2. Fitting window to shade.}

\item{subtitle}{Character. Overrides the default subtitle.}

\item{density}{Logical. Draw the ground-plot cloud as a WEIGHTED hexagonal
density instead of one point per plot, keeping the best-matched plots drawn
over it. Weighted via the \code{plot_weight} column if present, so the shading
reads as evidence rather than as sampling effort; without that column it
falls back to counts.}

\item{density_bins}{Integer. Bins across the x range of the hex grid.}

\item{density_points_max}{Integer. How many of the best-matched plots stay
drawn individually over the density. A COUNT rather than a fraction of the
maximum weight, because a fraction does not control the number drawn: the
weight distribution differs by species, and at 60\% of maximum one species
keeps 989 plots where another keeps 65.}
}
\value{
A \code{ggplot}.
}
\description{
Both trajectories are drawn over the same references, with the fitting window
shaded. This is the figure to look at when deciding whether a sweep result is
worth promoting: it shows what actually changes, over the part of the curve
the objective responds to.
}
\details{
The age-binned plot series is drawn as well, in blue. That series -- not the
scatter behind it -- is what the ground-plot term of the score is computed
against, so a candidate that looks wrong against the cloud but right against
the binned points is behaving exactly as scored.

Each binned point is sized by the number of plots behind it, because they
routinely differ by more than an order of magnitude and an equal-sized point
hides that completely. A bin holding a single plot is not a median of
anything, and the sharp reversals in the series are usually those bins.

The points are NOT joined by line segments. Connecting them asserts a
trajectory across ages where nothing was measured, and most of the movement
that line described came from the one-plot bins.

Passing \code{smooth} overlays a fit through the whole cloud with a confidence
band, for comparison only -- see \code{\link[=growth_smooth_observations]{growth_smooth_observations()}}. It is not
scored, and the legend says so. It shares the binned points' colour because
it summarizes the same observations; glyph, not hue, is what tells them apart.
}
\seealso{
Other growth calibration helpers:
\code{\link[=extract_landscape_cohort_structures]{extract_landscape_cohort_structures()}},
\code{\link[=growth_add_objective]{growth_add_objective()}},
\code{\link[=growth_auto_window]{growth_auto_window()}},
\code{\link[=growth_best_candidates]{growth_best_candidates()}},
\code{\link[=growth_bin_observations]{growth_bin_observations()}},
\code{\link[=growth_calibration_design]{growth_calibration_design()}},
\code{\link[=growth_calibration_partition]{growth_calibration_partition()}},
\code{\link[=growth_calibration_work_root]{growth_calibration_work_root()}},
\code{\link[=growth_climatic_distance]{growth_climatic_distance()}},
\code{\link[=growth_climatic_weight]{growth_climatic_weight()}},
\code{\link[=growth_expand_over_pseudo_species]{growth_expand_over_pseudo_species()}},
\code{\link[=growth_factorial_ratio_grid]{growth_factorial_ratio_grid()}},
\code{\link[=growth_fitting_windows]{growth_fitting_windows()}},
\code{\link[=growth_identifiability]{growth_identifiability()}},
\code{\link[=growth_inflation_factor]{growth_inflation_factor()}},
\code{\link[=growth_mortality_onset_frac]{growth_mortality_onset_frac()}},
\code{\link[=growth_plot_palette]{growth_plot_palette()}},
\code{\link[=growth_pseudo_species_name]{growth_pseudo_species_name()}},
\code{\link[=growth_reference_curves]{growth_reference_curves()}},
\code{\link[=growth_reference_inflection]{growth_reference_inflection()}},
\code{\link[=growth_score_fit]{growth_score_fit()}},
\code{\link[=growth_scoring_for]{growth_scoring_for()}},
\code{\link[=growth_smooth_observations]{growth_smooth_observations()}},
\code{\link[=growth_structure_design]{growth_structure_design()}},
\code{\link[=growth_window_for]{growth_window_for()}},
\code{\link[=plot_growth_calibration]{plot_growth_calibration()}},
\code{\link[=plot_growth_factorial_sensitivity]{plot_growth_factorial_sensitivity()}},
\code{\link[=read_growth_scoring]{read_growth_scoring()}},
\code{\link[=scale_linetype_growth_reference]{scale_linetype_growth_reference()}},
\code{\link[=write_growth_review_bundle]{write_growth_review_bundle()}}
}
\concept{growth calibration helpers}
