% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/growth_calibration_score.R
\name{growth_bin_observations}
\alias{growth_bin_observations}
\title{Condense a ground-plot cloud into an age-binned series}
\usage{
growth_bin_observations(
  obs,
  bin = 20L,
  probs = 0.5,
  site = NULL,
  weight = NULL
)
}
\arguments{
\item{obs}{A tibble with \code{age} and \code{aboveground_c_mg_ha}.}

\item{bin}{Numeric. Bin width in years.}

\item{probs}{Numeric. Quantile to take within each bin.}

\item{site}{Optional column name identifying the sampling location. When
given, repeated visits to one location are averaged within a bin before the
quantile is taken, so \code{n} counts locations rather than visits. Errors if the
named column is absent, rather than silently skipping the correction.}

\item{weight}{Optional column name holding a per-observation weight, typically
from \code{\link[=growth_climatic_weight]{growth_climatic_weight()}}. When given, the within-bin quantile is
weighted, so plots resembling the modelled landscape carry more of it. \code{n}
still counts observations; \code{weight} reports the weight behind each bin, so a
bin resting on many barely-relevant plots is visible as such.}
}
\value{
A tibble with \code{age} (bin mean), \code{value}, \code{n}, and \code{weight}.
}
\description{
Bins the observations on age and takes one quantile per bin, so every age
band contributes once no matter how many plots landed in it. This is the
non-parametric alternative to fitting a growth equation through the cloud: it
assumes nothing about curve shape, which matters because the shape being
tested is ForCS's own.
}
\details{
\code{probs = 0.5} (the median) tracks the central tendency of realized stands.
Raising it moves the series toward the upper envelope, which is arguably
where a fully stocked, single-cohort simulation belongs: the plots span every
stocking level, site quality and partial-disturbance history, and whole-plot
volume is attributed to the leading species, which holds a median of 69\% of
the stand here.

Where observations come from a permanent-plot network, pass \code{site} so that
each location contributes one value per bin. Permanent plots are remeasured
on a schedule that reflects program history rather than anything ecological
-- in the network this was built against, 78\% of locations carry more than one
visit and some carry thirteen -- so treating every visit as an independent
observation silently weights each bin toward whichever locations happen to
have been revisited most. That is pseudo-replication, and it biases the
quantile rather than merely tightening it.
}
\seealso{
Other growth calibration helpers:
\code{\link[=extract_landscape_cohort_structures]{extract_landscape_cohort_structures()}},
\code{\link[=growth_add_objective]{growth_add_objective()}},
\code{\link[=growth_auto_window]{growth_auto_window()}},
\code{\link[=growth_best_candidates]{growth_best_candidates()}},
\code{\link[=growth_calibration_design]{growth_calibration_design()}},
\code{\link[=growth_calibration_partition]{growth_calibration_partition()}},
\code{\link[=growth_calibration_work_root]{growth_calibration_work_root()}},
\code{\link[=growth_climatic_distance]{growth_climatic_distance()}},
\code{\link[=growth_climatic_weight]{growth_climatic_weight()}},
\code{\link[=growth_expand_over_pseudo_species]{growth_expand_over_pseudo_species()}},
\code{\link[=growth_factorial_ratio_grid]{growth_factorial_ratio_grid()}},
\code{\link[=growth_fitting_windows]{growth_fitting_windows()}},
\code{\link[=growth_identifiability]{growth_identifiability()}},
\code{\link[=growth_inflation_factor]{growth_inflation_factor()}},
\code{\link[=growth_mortality_onset_frac]{growth_mortality_onset_frac()}},
\code{\link[=growth_plot_palette]{growth_plot_palette()}},
\code{\link[=growth_pseudo_species_name]{growth_pseudo_species_name()}},
\code{\link[=growth_reference_curves]{growth_reference_curves()}},
\code{\link[=growth_reference_inflection]{growth_reference_inflection()}},
\code{\link[=growth_score_fit]{growth_score_fit()}},
\code{\link[=growth_scoring_for]{growth_scoring_for()}},
\code{\link[=growth_smooth_observations]{growth_smooth_observations()}},
\code{\link[=growth_structure_design]{growth_structure_design()}},
\code{\link[=growth_window_for]{growth_window_for()}},
\code{\link[=plot_growth_calibration]{plot_growth_calibration()}},
\code{\link[=plot_growth_candidate]{plot_growth_candidate()}},
\code{\link[=plot_growth_factorial_sensitivity]{plot_growth_factorial_sensitivity()}},
\code{\link[=read_growth_scoring]{read_growth_scoring()}},
\code{\link[=scale_linetype_growth_reference]{scale_linetype_growth_reference()}},
\code{\link[=write_growth_review_bundle]{write_growth_review_bundle()}}
}
\concept{growth calibration helpers}
