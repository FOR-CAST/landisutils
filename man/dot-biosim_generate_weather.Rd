% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/climate_data.R
\name{.biosim_generate_weather}
\alias{.biosim_generate_weather}
\title{Robust \code{BioSIM::generateWeather()} wrapper: request staggering + exponential-backoff retry}
\usage{
.biosim_generate_weather(...)
}
\arguments{
\item{...}{passed verbatim to \code{\link[BioSIM:generateWeather]{BioSIM::generateWeather()}}.}
}
\value{
the \code{\link[BioSIM:generateWeather]{BioSIM::generateWeather()}} result; errors after exhausting \code{max_attempts}.
}
\description{
The BioSIM web API can be slow or transiently unavailable when many requests arrive at once, and the
\code{BioSIM} R client exposes no timeout/retry/delay knobs (\code{biosimclient.config()} only tunes
\code{nbNearestNeighbours} and climate-generation/test flags). A bare \code{generateWeather()} call therefore has
no protection: a transient failure aborts the whole fetch, and a stalled call hangs indefinitely. This
wrapper adds (all tunable via \code{options()}):
\itemize{
\item a random pre-request \strong{stagger delay} so concurrent \code{furrr} workers don't hit the server in
lockstep -- \code{landisutils.biosim.request_delay} = \code{c(min, max)} seconds (default \code{c(1, 5)});
\item \strong{exponential backoff with jitter} on failure -- \code{landisutils.biosim.max_attempts} (default
\code{5L}) total tries, waiting \code{landisutils.biosim.backoff_base * 2^(attempt - 1)} seconds (base
default \code{15}) between tries, so repeated failures back off progressively (gentler on an
overwhelmed server);
\item an optional \strong{per-attempt timeout} -- \code{landisutils.biosim.timeout} seconds (default \code{Inf} =
off) via \code{\link[base:setTimeLimit]{base::setTimeLimit()}}; on timeout the J4R client is reset
(\code{\link[BioSIM:shutdownClient]{BioSIM::shutdownClient()}}) before the next try so a stalled socket can't poison it.
}
}
\details{
NOTE: these options are read at call time INSIDE the fetch (which often runs in a \code{furrr}/\code{crew}
worker), so to tune them set the \code{options()} somewhere the worker inherits -- e.g. the project
\code{.Rprofile} -- NOT a file only the main process sources (a \code{targets} \verb{_local.R}).
}
\keyword{internal}
