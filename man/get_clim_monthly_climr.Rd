% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/climate_monthly_climr.R
\name{get_clim_monthly_climr}
\alias{get_clim_monthly_climr}
\title{Fetch one year of monthly climr weather and write Arrow CSV partitions}
\usage{
get_clim_monthly_climr(
  year,
  xyz,
  vars,
  studyArea_hash,
  obs_ts_dataset = "climatena",
  path = .climateCachePath(),
  gcms = NULL,
  ssps = NULL,
  max_run = 0L
)
}
\arguments{
\item{year}{integer single calendar year to fetch. In projection mode, must
lie within \code{\link[climr:data-option-lists]{climr::list_gcm_ssp_years()}}.}

\item{xyz}{\code{data.frame} with columns \code{lon}, \code{lat}, \code{elev}, \code{id} (one row per
cell). See \code{.climr_build_xyz()} (internal) or build manually.}

\item{vars}{character vector of lowercase variable names (e.g.
\code{c("prcp", "tmax", "tmin")}); see \code{\link[=prep_monthly_weather_climr]{prep_monthly_weather_climr()}} for the
supported set.}

\item{studyArea_hash}{character. Short hash of the study-area object (see
\code{.studyArea_hash()}) used as a cache subdirectory so that distinct study
areas don't collide.}

\item{obs_ts_dataset}{character. climr observational time-series dataset.
One of \code{"climatena"} or \code{"cru.gpcc"}. Default \code{"climatena"}. Ignored when
\code{gcms} is set.}

\item{path}{character. Directory under which the \verb{Climr_Monthly/} arrow
dataset is written. Default uses the package climate cache
(\code{getOption("landisutils.cache.path")}).}

\item{gcms}{character vector of CMIP6 GCM names (subset of
\code{\link[climr:data-option-lists]{climr::list_gcms()}}). When \code{NULL} (default), runs in observational mode.
When set, runs in projection mode and \code{ssps} is required.}

\item{ssps}{character vector of CMIP6 SSP names (subset of
\code{\link[climr:data-option-lists]{climr::list_ssps()}}, e.g. \code{"ssp245"}). Required when \code{gcms} is set.}

\item{max_run}{integer number of ensemble runs per GCM to fetch. With
\code{ensemble_mean = TRUE} (climr's default and what we use), \code{max_run = 0L}
returns only the per-GCM ensemble mean. Default \code{0L}.}
}
\value{
Path (character) to the scenario-tagged dataset directory (i.e.
the directory containing the \code{Variable=...} partitions).
}
\description{
Wraps \code{\link[climr:downscale]{climr::downscale()}} for one calendar \code{year} against the points in
\code{xyz}, then splits the long-format output by base variable and writes Arrow
CSV partitions under
\verb{path/Climr_Monthly/<studyArea_hash>/<scenario_tag>/}. Partitions are keyed
by \code{Variable}/\code{Year} in observational mode and additionally by \code{GCM}/\code{SSP}
in projection mode. Existing per-variable / per-year partition trees are
detected via \code{\link[=dir.exists]{dir.exists()}} and skipped, so re-runs are cheap.
}
\details{
The function runs in two modes:
\describe{
\item{Observational (default)}{\code{gcms = NULL}. Uses \code{obs_years = year} and
\code{obs_ts_dataset} for ClimateNA-style historical anomalies.}
\item{Projection}{\code{gcms} set (e.g. \code{\link{climr_ensemble_8}}) and \code{ssps} set
(e.g. \code{"ssp245"}). Uses \code{gcm_ssp_years = year} to fetch CMIP6 GCM
time-series projections; \code{max_run} controls how many runs per GCM are
averaged (with \code{ensemble_mean = TRUE}, climr's default).}
}

Per the climr API, the climr cache itself is configured by setting
\code{options("climr.cache.path" = ...)} (climr GitHub issue 274). The orchestrator
\code{\link[=prep_monthly_weather_climr]{prep_monthly_weather_climr()}} sets a sensible default via
\code{\link[withr:with_options]{withr::local_options()}} for the duration of a call; if you call this
helper directly you should do the same.
}
\section{Topographic considerations}{

The climate data sources used by this package (BioSIM, Daymet, TerraClim)
are \strong{not PRISM-derived}.
PRISM (\url{https://prism.oregonstate.edu/}) applies topographically-aware
interpolation (slope, aspect, elevation, coastal proximity, temperature
inversions) when downscaling station observations to a grid
(Daly et al. 2008); the sources wrapped here use simpler interpolation
schemes that can produce \strong{large discrepancies in areas of complex
topography} (mountainous terrain, steep elevation gradients, rain
shadows): inter-product differences of 5-60\% in annual precipitation
(Henn et al. 2018) and >6 \eqn{^\circ}C in temperature (Walton & Hall
2018) have been documented in the western US.
Carefully evaluate the suitability of the chosen source for your study
area, especially in topographically heterogeneous landscapes.
}

\references{
Daly, C., Halbleib, M., Smith, J.I., Gibson, W.P., Doggett, M.K.,
Taylor, G.H., Curtis, J., & Pasteris, P.P. (2008). Physiographically
sensitive mapping of climatological temperature and precipitation across
the conterminous United States. \emph{International Journal of Climatology},
28(15), 2031-2064. \doi{10.1002/joc.1688}

Henn, B., Newman, A.J., Livneh, B., Daly, C., & Lundquist, J.D. (2018).
An assessment of differences in gridded precipitation datasets in complex terrain.
\emph{Journal of Hydrology}, 556, 1205-1219. \doi{10.1016/j.jhydrol.2017.03.008}
}
\seealso{
\code{\link[=assemble_climate_library_file_monthly_climr]{assemble_climate_library_file_monthly_climr()}},
\code{\link[=prep_monthly_weather_climr]{prep_monthly_weather_climr()}}, \code{\link{climr_ensemble_8}}
}
