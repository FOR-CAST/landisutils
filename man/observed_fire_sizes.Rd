% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_dynamic_fire.R
\name{observed_fire_sizes}
\alias{observed_fire_sizes}
\title{Observed per-fire sizes: one per ignition point, upgraded to mapped area}
\usage{
observed_fire_sizes(points, polys = NULL, min_size_ha = 0)
}
\arguments{
\item{points}{SpatVector. Ignition points with \code{SIZE_HA} and \code{YEAR} columns.}

\item{polys}{SpatVector or NULL. Perimeter polygons with \code{SIZE_HA} and \code{YEAR}
columns. NULL keeps every point's own size.}

\item{min_size_ha}{Numeric scalar. Sizes below this, and missing sizes, are
dropped. Default \code{0} keeps every positive and zero size.}
}
\value{
Numeric vector of sizes (ha), sorted ascending; at most one element
per point.
}
\description{
One size per ignition point. Each point keeps its own \code{SIZE_HA} unless a
perimeter polygon from the SAME calendar year contains it, in which case the
polygon's \code{SIZE_HA} replaces it. For NFDB points and NBAC perimeters, this
keeps NFDB's full sample (pre-1972 fires, and small fires NBAC does not map)
while taking NBAC's satellite-derived area wherever a fire was mapped.
}
\details{
This is the fire-size rule behind \code{\link[=save_observed_fire_targets]{save_observed_fire_targets()}}'s
\code{fire_sizes_ha}. It is exported so that anything else derived from the same
fire record -- such as a fitted fire-size distribution -- uses the same sizes
as the calibration's size target, rather than a second rule that can drift
from it. Binding points and polygons as separate rows instead would count
every mapped fire twice.
}
\seealso{
Other Dynamic Fire calibration helpers:
\code{\link[=apply_calibrated_hi_prop]{apply_calibrated_hi_prop()}},
\code{\link[=apply_calibrated_ignprob]{apply_calibrated_ignprob()}},
\code{\link[=apply_calibrated_num_fires]{apply_calibrated_num_fires()}},
\code{\link[=bc_fuel_code_to_base]{bc_fuel_code_to_base()}},
\code{\link[=build_calibration_scenario_template]{build_calibration_scenario_template()}},
\code{\link[=build_calibration_spinup_scenario]{build_calibration_spinup_scenario()}},
\code{\link[=calibrate_dynamic_fire]{calibrate_dynamic_fire()}},
\code{\link[=calibration_par_names]{calibration_par_names()}},
\code{\link[=dedup_community_snapshot]{dedup_community_snapshot()}},
\code{\link[=default_severity_prior_sturtevant2009]{default_severity_prior_sturtevant2009()}},
\code{\link[=loss_from_stats]{loss_from_stats()}},
\code{\link[=parse_dynamic_fire_logs]{parse_dynamic_fire_logs()}},
\code{\link[=patch_fire_config]{patch_fire_config()}},
\code{\link[=run_calibration_spinup]{run_calibration_spinup()}},
\code{\link[=run_calibration_validation]{run_calibration_validation()}},
\code{\link[=save_observed_fire_targets]{save_observed_fire_targets()}},
\code{\link[=sim_landis]{sim_landis()}},
\code{\link[=sim_mock]{sim_mock()}},
\code{\link[=sim_r_reimpl]{sim_r_reimpl()}}
}
\concept{Dynamic Fire calibration helpers}
