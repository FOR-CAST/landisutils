% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fps_run.R
\name{fps_run_docker}
\alias{fps_run_docker}
\title{Run the Forest Product Sector Module in a Docker container}
\usage{
fps_run_docker(
  run_dir,
  config_file = "fps.txt",
  image = NULL,
  assembly = "/opt/fps/Landis.Extension.FPS-v1.dll",
  console = NULL,
  pull = FALSE,
  cpu_limit = 1,
  mem_limit = "1g",
  error_on_log = TRUE,
  check_headers = TRUE
)
}
\arguments{
\item{run_dir}{Character. Directory holding the configuration and flux logs;
bind-mounted as the container working directory.}

\item{config_file}{Character. Configuration file name, relative to \code{run_dir}.}

\item{image}{Character. Container image. Defaults to the
\code{landisutils.fps.image} option.}

\item{assembly}{Character. Path to the FPSM assembly inside the image. Passed
explicitly with \verb{--entrypoint dotnet} rather than relying on the image's own
\code{ENTRYPOINT}, so the function works with any image carrying dotnet and the
assembly.}

\item{console}{Character. Path to the \code{docker} executable; defaults to
\code{\link[=landis_find_docker]{landis_find_docker()}}.}

\item{pull}{Logical. \verb{docker pull} before running, so the captured digest
reflects the registry rather than a possibly stale local copy.}

\item{cpu_limit, mem_limit}{Resource caps. FPSM is single-threaded and peaks
well under 64 MB even on a 400-year replicate, so the defaults are generous.
\code{NULL} or \code{Inf} omits the corresponding flag.}

\item{error_on_log}{Logical. Fail when \code{FPS_log.txt} is non-empty.}

\item{check_headers}{Logical. Perform the flux-log header assertion.}
}
\value{
Character vector of paths to the files in \code{\link[=fps_output_files]{fps_output_files()}}, in
that order, suitable for a \code{targets::tar_target(format = "file")}.
}
\description{
Runs FPSM over one directory containing a configuration file and the two ForCS
flux logs it names, with that directory bind-mounted as the container's working
directory. FPSM writes \code{\link[=fps_output_files]{fps_output_files()}} there under fixed names, so each
run needs its own directory.
}
\details{
Three pre-flight checks run before the container starts, each guarding a
failure mode that is otherwise silent or obscure:
\itemize{
\item the input files named in the configuration must exist \strong{with exactly that
case}. The shipped FPSM examples name \code{log_fluxDOM.csv} beside a file
called \code{log_FluxDOM.csv}, which works on Windows and aborts on Linux.
\item each flux log's header must still match the positions FPSM indexes (see
\code{.fps_flux_columns}), because FPSM performs no header validation and would
otherwise read a reordered column as the wrong quantity.
\item on completion, a non-empty \code{FPS_log.txt} is treated as an error by default.
That file collects the \emph{non-fatal} problems FPSM detects, which include carbon that
was never allocated to any pool, so a silent run is the only acceptable one.
}
}
\seealso{
\code{\link[=landis_run_docker]{landis_run_docker()}}, \code{\link[=landis_find_docker]{landis_find_docker()}}

Other FPSM helpers:
\code{\link[=fps_output_files]{fps_output_files()}}
}
\concept{FPSM helpers}
