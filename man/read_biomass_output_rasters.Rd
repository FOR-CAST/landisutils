% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_transitions.R
\name{read_biomass_output_rasters}
\alias{read_biomass_output_rasters}
\title{Read Output.Biomass raster snapshots}
\usage{
read_biomass_output_rasters(
  dirs,
  times,
  species,
  live_map_pattern = "outputs/biomass/biomass-{species}-{timestep}.tif",
  run_name = NULL
)
}
\arguments{
\item{dirs}{Character vector of replicate root directories (one per replicate),
each containing output rasters at the path given by \code{live_map_pattern}.}

\item{times}{Integer vector of snapshot years to extract.}

\item{species}{Character vector of species names to include.}

\item{live_map_pattern}{Character.  File naming pattern for live-biomass rasters,
using \code{{species}} and \code{{timestep}} as literal placeholders (same notation as
the \code{MapNames} directive in the Output Biomass extension config file).
Default: \code{"outputs/biomass/biomass-{species}-{timestep}.tif"}.}

\item{run_name}{Character.  Scenario label attached as the \code{scenario} column.
If \code{NULL} (default), derived from \code{basename(dirname(dirs[1]))}.}
}
\value{
A \code{data.table} with columns
\verb{scenario, replicate, Time, row, column, species, biomass}
where \code{biomass} is total live biomass in Mg C ha^-1.
The \code{ecoregion} column present in \code{\link[=read_biomass_c_snapshots]{read_biomass_c_snapshots()}} output is
absent here (rasters do not carry ecoregion attribution per cell), but all
downstream functions treat \code{ecoregion} as optional.
This is the canonical format accepted by all downstream functions
(\code{\link[=leading_species]{leading_species()}}, \code{\link[=community_label]{community_label()}}, \code{\link[=biomass_landscape_summary]{biomass_landscape_summary()}}).
}
\description{
\strong{General-purpose reader.} Reads per-species biomass rasters written by the
LANDIS-II Output Biomass v4 extension (\code{Output.Biomass-v4}) and returns
per-cell, per-species total live biomass at the requested snapshot years.
}
\details{
This reader works with any LANDIS-II succession extension (Biomass Succession,
ForCS, NECN, etc.) as long as Output.Biomass is included in the scenario.
Be aware that writing many raster outputs can slow down the simulation;
for ForCS scenarios, \code{\link[=read_biomass_c_snapshots]{read_biomass_c_snapshots()}} avoids this overhead by
reading the per-cohort carbon log that ForCS writes unconditionally.

Raster values are assumed to be in g C m^-2 and are converted to Mg C ha^-1
(multiply by 0.01).  This is correct for ForCS; for Biomass Succession the
rasters store g dry biomass m^-2, so apply an appropriate carbon fraction
before downstream use.
}
\seealso{
\code{\link[=read_biomass_c_snapshots]{read_biomass_c_snapshots()}} for the ForCS-specific fast path.

Other Vegetation transition helpers:
\code{\link[=biomass_landscape_summary]{biomass_landscape_summary()}},
\code{\link[=community_label]{community_label()}},
\code{\link[=leading_species]{leading_species()}},
\code{\link[=plot_species_biomass]{plot_species_biomass()}},
\code{\link[=plot_transitions]{plot_transitions()}},
\code{\link[=read_biomass_c_snapshots]{read_biomass_c_snapshots()}},
\code{\link[=transition_data]{transition_data()}}
}
\concept{Vegetation transition helpers}
