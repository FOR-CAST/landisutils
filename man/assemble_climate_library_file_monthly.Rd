% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/climate_monthly_biosim.R
\name{assemble_climate_library_file_monthly}
\alias{assemble_climate_library_file_monthly}
\title{Assemble LANDIS-II Climate Library wide-format table from a monthly BioSIM Arrow dataset}
\usage{
assemble_climate_library_file_monthly(
  dataset_path,
  vars,
  id_col = "EcoID",
  cell_eco = NULL,
  cell_ids = NULL
)
}
\arguments{
\item{dataset_path}{character. Path to the \code{Climatic_Monthly} Arrow dataset
directory (containing \code{YEAR=...}/\code{BatchID=...}/\verb{part-0.csv} partitions).}

\item{vars}{character vector of BioSIM column names to retain (e.g.
\code{c("TotalPrcp", "MeanTmin", "MeanTmax", "WndS", "WndD")}).}

\item{id_col}{character. Name of the ecoregion-id column. Default \code{"EcoID"}.}

\item{cell_eco}{optional data.frame mapping \code{CellID} -> ecoregion id (\code{id_col}). Supply for the GLOBAL
per-cell store (which is climate-only, no \code{EcoID}): the ecoregion grouping is study-area-specific and
is applied here by joining this map. When \code{NULL}, the dataset is assumed to already carry \code{id_col}.}

\item{cell_ids}{optional integer vector of \code{CellID}s to retain (the calling study area's cells); used
to filter a shared global store down to one study area before summarising. \code{NULL} keeps all cells.}
}
\value{
\code{tbl_df} with columns \code{Year}, \code{Month}, \code{Variable}, and one
column per ecoregion id.
}
\description{
Reads the partitioned Arrow CSV dataset written by \code{\link[=get_clim_monthly]{get_clim_monthly()}},
summarizes the requested variables by \verb{(Year, Month, EcoID)}, applies unit
conversions to match LANDIS-II conventions, and pivots to the wide format
ingested by \code{LandisClimateConfig}. The result has the same shape that
\code{\link[=prep_monthly_weather]{prep_monthly_weather()}} returns and is suitable for \code{\link[=writeClimateData]{writeClimateData()}}.
}
\details{
Unit conversions applied:
\itemize{
\item \code{TotalPrcp} (mm) \eqn{\to} cm (\eqn{\div 10})
\item \code{WndS} (km/h) \eqn{\to} m/s (\eqn{\div 3.6})
}

BioSIM \code{WndD} is reported as the wind \emph{from-direction} (degrees), which
is what the LANDIS-II Climate Library expects. The monthly value here is a
frequency-weighted circular mean across the model's 36 direction bins (see
\code{\link[=get_clim_monthly]{get_clim_monthly()}}); when summarising across cells within an ecoregion
the same arithmetic-mean convention as the daily pipeline is applied.
}
\seealso{
\code{\link[=get_clim_monthly]{get_clim_monthly()}}, \code{\link[=prep_monthly_weather_biosim]{prep_monthly_weather_biosim()}},
\code{\link[=writeClimateData]{writeClimateData()}}
}
