% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tar_landis.R
\name{landis_archive_rep}
\alias{landis_archive_rep}
\title{Move a completed LANDIS-II replicate from scratch to its final location}
\usage{
landis_archive_rep(run_dir, final_dir, max_tries = 5L, backoff_sec = 5)
}
\arguments{
\item{run_dir}{Character. The completed replicate directory to move (source).}

\item{final_dir}{Character. The destination directory (created if needed).}

\item{max_tries}{Integer. Maximum \code{rsync} attempts before giving up
(default \code{5}).}

\item{backoff_sec}{Numeric. Base seconds for linear backoff between attempts
(attempt \code{i} waits \code{backoff_sec * i}; default \code{5}).}
}
\value{
Character. \code{final_dir}, on success.
}
\description{
Moves a finished replicate directory \code{run_dir} (typically on fast, local,
Docker-bind-mountable scratch) to \code{final_dir} (typically a slower, networked
final/archive location such as an NFS project share) in a way that is
fault-tolerant \emph{and} all-or-nothing at the destination:
}
\details{
\enumerate{
\item \code{rsync -a --partial} copies \code{run_dir} into a sibling staging directory
\code{paste0(final_dir, ".partial")} on the \emph{destination} filesystem, with
retry + linear backoff so a transient network blip does not abort the run
(\code{--partial} keeps partially transferred files so a retry resumes rather
than restarts). Crucially this is a COPY -- the scratch source stays the
complete, authoritative replicate until the destination is verified, so a
total transfer failure loses nothing.
\item After a verified \code{rsync} exit status of \code{0}, the staging directory is
published with an atomic \code{rename} into \code{final_dir}. Because the rename is
atomic, \code{final_dir} only ever appears \emph{complete} -- a partial transfer is
never visible to a downstream skip-check that reads \code{final_dir}. (This is
why \code{--remove-source-files} is deliberately NOT used: it would delete
scratch incrementally and could leave a half-emptied source on total
failure, and a partial destination could be mistaken for a complete run.)
\item Only once \code{final_dir} is in place is the scratch source deleted.
}

If \code{run_dir} and \code{final_dir} already resolve to the same path (no scratch in
use), this is a no-op that returns \code{final_dir} without copying or deleting.

Requires the \code{rsync} executable on \code{PATH} (standard on Linux/macOS). A
missing \code{rsync}, or repeated failures, raises an error after \code{max_tries}
attempts so the run target fails loudly and the scratch copy is retained for
inspection rather than silently lost.
}
\seealso{
\code{\link[=tar_landis]{tar_landis()}}, \code{\link[=landis_run_docker]{landis_run_docker()}}, \code{\link[=landis_replicate]{landis_replicate()}}

Other LANDIS-II execution helpers:
\code{\link[=host_cpu_info]{host_cpu_info()}},
\code{\link[=landis_find]{landis_find()}},
\code{\link[=landis_find_docker]{landis_find_docker()}},
\code{\link[=landis_pool_exec]{landis_pool_exec()}},
\code{\link[=landis_pool_restart_one]{landis_pool_restart_one()}},
\code{\link[=landis_pool_start]{landis_pool_start()}},
\code{\link[=landis_pool_stop]{landis_pool_stop()}},
\code{\link[=landis_replicate]{landis_replicate()}},
\code{\link[=landis_run_docker]{landis_run_docker()}},
\code{\link[=landis_run_local]{landis_run_local()}},
\code{\link[=read_landis_resource_logs]{read_landis_resource_logs()}},
\code{\link[=tar_landis]{tar_landis()}},
\code{\link[=write_landis_scenario_file]{write_landis_scenario_file()}}
}
\concept{LANDIS-II execution helpers}
