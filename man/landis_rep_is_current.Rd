% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landis_input_hash.R
\name{landis_rep_is_current}
\alias{landis_rep_is_current}
\title{Is a LANDIS-II replicate already complete for these inputs?}
\usage{
landis_rep_is_current(
  rep_dir,
  dep_files,
  base_seed,
  rep_index,
  scenario_file,
  force = FALSE
)
}
\arguments{
\item{rep_dir}{Character(1). The replicate directory.}

\item{dep_files}{Character. The dependency files staged into the replicate.}

\item{base_seed}{Integer. The base random seed.}

\item{rep_index}{Integer. The replicate index.}

\item{scenario_file}{Character(1). The scenario file name.}

\item{force}{Logical(1). \code{TRUE} never counts a replicate as current.}
}
\value{
Logical(1).
}
\description{
\code{TRUE} when \code{Landis-log.txt} reports a completed run and \code{log/input_hash.json} records the input
hash of \code{dep_files} et al. Besides the current \code{\link[=landis_input_hash]{landis_input_hash()}}, the hash landisutils 0.0.149
and earlier would have written is accepted, sorted in this process's locale or under \code{C.UTF-8}
(ICU). A replicate finished by an earlier version is therefore not re-simulated after an upgrade,
whichever of those locales wrote it.
}
