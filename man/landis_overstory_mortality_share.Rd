% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_dynamic_fire.R
\name{landis_overstory_mortality_share}
\alias{landis_overstory_mortality_share}
\title{Share of burned area that lost its dominant cohort}
\usage{
landis_overstory_mortality_share(rep_dir)
}
\arguments{
\item{rep_dir}{Character. Path to the replicate directory.}
}
\value{
A list with \code{burned_cells}, \code{high_cells} and \code{share} (NA when nothing burned), or NULL
when the replicate holds no severity maps or is missing one of the files above.
}
\description{
The proportion of a replicate's burned cells in which the cohort holding the most biomass was
killed. This is the model-side counterpart of a field burn-severity class defined by mortality
of the structurally dominant vegetation, and unlike the extension's own severity classes -- which
are crown fraction burned -- it is comparable with an observed mortality measure. In a forest
that burns hot at ground level without crowning, the two disagree by construction.
}
\details{
Everything needed is in the replicate directory, because a run is a copy of its scenario:
\itemize{
\item \verb{fire/severity-\{t\}.tif} -- the severity map per timestep. The Dynamic Fire encoding is
0 inactive, 1 active and unburned, 2 burned with no cohort damaged, and severity + 2 for
a damaged cell, so a cell's severity class is its map value MINUS TWO.
\item \code{initial-communities.csv} / \code{.tif} -- the cohorts on each cell.
\item \code{species.txt} -- longevity, which the damage table's age thresholds are shares of.
\item \code{DynamicFire_Spp_Table.csv} -- each species' fire tolerance.
\item \code{dynamic-fire.txt} -- the \code{FireDamageTable}, mapping severity minus tolerance to the
oldest cohort killed, as a percentage of longevity.
}

A cohort dies when severity is 5 (the extension kills everything at that severity, whatever the
tolerance) or when its age is at or below that percentage of its species' longevity. Composition
is read at time zero, so a cell that burns twice in one replicate is scored on its original
cohorts; in a calibration run, where succession is frozen, only reburns are affected.
}
\seealso{
Other Dynamic Fire calibration helpers:
\code{\link[=apply_calibrated_damage_age]{apply_calibrated_damage_age()}},
\code{\link[=apply_calibrated_hi_prop]{apply_calibrated_hi_prop()}},
\code{\link[=apply_calibrated_ignprob]{apply_calibrated_ignprob()}},
\code{\link[=apply_calibrated_num_fires]{apply_calibrated_num_fires()}},
\code{\link[=bc_fuel_code_to_base]{bc_fuel_code_to_base()}},
\code{\link[=build_calibration_scenario_template]{build_calibration_scenario_template()}},
\code{\link[=build_calibration_spinup_scenario]{build_calibration_spinup_scenario()}},
\code{\link[=calibrate_dynamic_fire]{calibrate_dynamic_fire()}},
\code{\link[=calibration_par_names]{calibration_par_names()}},
\code{\link[=dedup_community_snapshot]{dedup_community_snapshot()}},
\code{\link[=default_severity_prior_sturtevant2009]{default_severity_prior_sturtevant2009()}},
\code{\link[=loss_from_stats]{loss_from_stats()}},
\code{\link[=observed_fire_sizes]{observed_fire_sizes()}},
\code{\link[=parse_dynamic_fire_logs]{parse_dynamic_fire_logs()}},
\code{\link[=patch_fire_config]{patch_fire_config()}},
\code{\link[=run_calibration_spinup]{run_calibration_spinup()}},
\code{\link[=run_calibration_validation]{run_calibration_validation()}},
\code{\link[=save_observed_fire_targets]{save_observed_fire_targets()}},
\code{\link[=sim_landis]{sim_landis()}},
\code{\link[=sim_mock]{sim_mock()}},
\code{\link[=sim_r_reimpl]{sim_r_reimpl()}}
}
\concept{Dynamic Fire calibration helpers}
